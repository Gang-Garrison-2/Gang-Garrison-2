<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<event category="STEP" id="1">
  <actions>
    <action id="603" library="1">
      <!--action name: Code-->
      <kind>CODE</kind>
      <allowRelative>false</allowRelative>
      <question>false</question>
      <canApplyTo>true</canApplyTo>
      <actionType>CODE</actionType>
      <functionName/>
      <relative>false</relative>
      <not>false</not>
      <appliesTo>.self</appliesTo>
      <arguments>
        <argument kind="STRING">var mapHeight;
mapHeight = map_height();

if (floor(image_index) == (image_number - 1))
    image_speed = 0;


// Move down to meet the ground
// We use two different loops for efficiency

wallSetSolid();
//while (place_free(x,y-(y mod 6)+6) and y &lt; mapHeight)
//    y += 6;

while (place_free(x,y+1) and y &lt; mapHeight)
    y += 1;
wallUnsetSolid();

//if (place_free(x, y + 1))
//    move_contact_solid(270, mapHeight - y);
</argument>
      </arguments>
    </action>
  </actions>
</event>
