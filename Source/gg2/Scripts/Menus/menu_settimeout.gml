// sets timeout before menu can be clicked
// argument0 - object (usually the calling object)
// argument1 - number of steps

with (argument0)
{
    canClick = false;
    alarm[0] = argument1;
}
