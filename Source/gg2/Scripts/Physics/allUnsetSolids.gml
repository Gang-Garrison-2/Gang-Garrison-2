with(Obstacle)
    solid = false;
with(IntelGate)
    solid = false;
with(TeamGate)
    solid = false;
with(ControlPointSetupGate)
    solid = false;
with(PlayerWall)
    solid = false;
with(BulletWall)
    solid = false;

