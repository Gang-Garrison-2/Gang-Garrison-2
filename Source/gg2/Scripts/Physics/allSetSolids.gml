with(Obstacle)
    solid = true;
with(IntelGate)
    solid = true;
with(TeamGate)
    solid = true;
with(ControlPointSetupGate)
    solid = true;
with(PlayerWall)
    solid = true;
with(BulletWall)
    solid = !open;

