refactor(enigma): replace GM8-only runtime introspection

diff --git a/Source/gg2/Objects/Alternative Objects/2013 Halloween Mini-Haxxy Rewards/Ghost.events/Step.xml b/Source/gg2/Objects/Alternative Objects/2013 Halloween Mini-Haxxy Rewards/Ghost.events/Step.xml
index 71a18c16..f010916d 100644
--- a/Source/gg2/Objects/Alternative Objects/2013 Halloween Mini-Haxxy Rewards/Ghost.events/Step.xml	
+++ b/Source/gg2/Objects/Alternative Objects/2013 Halloween Mini-Haxxy Rewards/Ghost.events/Step.xml	
@@ -65,7 +65,8 @@ if (owner.object != -1) {
         dist = point_distance(x, y, other.origX, other.origY);
         if (dist &lt; closestDist or closestDist == -1)
         {
-            if (!cloak)
+            // TODO(enigma): self. needed inside with(); ENIGMA resolves bare names against the outer object
+            if (!self.cloak)
             {
                 closest = id;
                 closestDist = dist;
diff --git a/Source/gg2/Objects/GameElements/Server.events/Create.xml b/Source/gg2/Objects/GameElements/Server.events/Create.xml
index 5bf2fed8..839b803e 100644
--- a/Source/gg2/Objects/GameElements/Server.events/Create.xml
+++ b/Source/gg2/Objects/GameElements/Server.events/Create.xml
@@ -18,9 +18,7 @@ pingRunning = false;
 pingFinished = false;
 infos = ds_map_create();
 
-if(!variable_global_exists("runningPingCount")) {
-    global.runningPingCount = 0;
-}
+// global.runningPingCount is initialized in game_init
 </argument>
       </arguments>
     </action>
diff --git a/Source/gg2/Objects/InGameElements/Builder.events/User Event 2.xml b/Source/gg2/Objects/InGameElements/Builder.events/User Event 2.xml
index d77ef2af..d494446e 100644
--- a/Source/gg2/Objects/InGameElements/Builder.events/User Event 2.xml	
+++ b/Source/gg2/Objects/InGameElements/Builder.events/User Event 2.xml	
@@ -15,6 +15,8 @@
       <arguments>
         <argument kind="STRING">// change preview image etc when a new entity is selected.
 
+// button was an unintended instance variable; local like in Step/Draw.
+var button;
 button = ds_list_find_value(global.entityData, entityButtons[selected, INDEX]);
 selectedSprite = ds_map_find_value(button, "entity_sprite");
 selectedImage = ds_map_find_value(button, "entity_image");
diff --git a/Source/gg2/Objects/InGameElements/Character.events/Begin Step.xml b/Source/gg2/Objects/InGameElements/Character.events/Begin Step.xml
index bd1550b5..1cc8d358 100644
--- a/Source/gg2/Objects/InGameElements/Character.events/Begin Step.xml	
+++ b/Source/gg2/Objects/InGameElements/Character.events/Begin Step.xml	
@@ -38,7 +38,8 @@ if(vspeed &gt;= 0)
                 if(!place_meeting(x, y-1, other)) continue; // We can only stand on platforms immediately below us
                 if(place_meeting(x, y, other)) continue;    // We can't stand on platforms we already collide with
                 other.onground = true;
-                if(resetMoveStatus)
+                // TODO(enigma): self. needed inside with(); ENIGMA resolves bare names against the outer object
+                if(self.resetMoveStatus)
                     onNonSurfingGround = true;
                 // If we didn't find a nonsurfing dropdown yet, we have to keep looking
                 // because there may still be one overlapping the surfing dropdown we found
@@ -190,7 +191,7 @@ if(rocketJumping)
 {
     if(global.run_virtual_ticks)
     {
-        if !variable_local_exists("jumpFlameParticleType")
+        if (jumpFlameParticleType == -1)
         {
             jumpFlameParticleType = part_type_create();
             part_type_sprite(jumpFlameParticleType,FlameS,true,false,true);
@@ -199,7 +200,7 @@ if(rocketJumping)
             part_type_scale(jumpFlameParticleType,0.7,-0.65);
         }
         
-        if !variable_global_exists("jumpFlameParticleSystem")
+        if (global.jumpFlameParticleSystem == -1)
         {
             global.jumpFlameParticleSystem = part_system_create();
             part_system_depth(global.jumpFlameParticleSystem, 10);
@@ -225,7 +226,7 @@ if(rocketJumping)
 if(spinjumping)
 {
     
-    if !variable_local_exists("jumpDustParticleType")
+    if (jumpDustParticleType == -1)
     {
         jumpDustParticleType = part_type_create();
         part_type_sprite(jumpDustParticleType,SpeedBoostS,false,false,true);
@@ -234,7 +235,7 @@ if(spinjumping)
         part_type_scale(jumpDustParticleType,1,1);
         part_type_orientation(jumpDustParticleType, -90, -90, 0, 0, 0);
     }
-    if !variable_global_exists("jumpDustParticleSystem")
+    if (global.jumpDustParticleSystem == -1)
     {
         global.jumpDustParticleSystem = part_system_create();
         part_system_depth(global.jumpDustParticleSystem, 10);
@@ -396,7 +397,7 @@ if ubered {
     }
     if instance_exists(currentWeapon) {
         with(currentWeapon) {
-            if(variable_local_exists("maxAmmo")) {
+            if(maxAmmo != -1) {
                 ammoCount = maxAmmo;
             }
             if (alarm[5] &gt; 0)
@@ -419,7 +420,7 @@ if (instance_exists(ArenaHUD)) {
 if ((arenaRoundEnd or global.mapchanging) and !player.humiliated) {
     if instance_exists(currentWeapon) {
         with(currentWeapon){
-            if(variable_local_exists("maxAmmo")) {
+            if(maxAmmo != -1) {
                 ammoCount = maxAmmo;
             }
             if (alarm[5] &gt; 0)
diff --git a/Source/gg2/Objects/InGameElements/Character.events/Create.xml b/Source/gg2/Objects/InGameElements/Character.events/Create.xml
index bfa8db24..7abc22a5 100644
--- a/Source/gg2/Objects/InGameElements/Character.events/Create.xml
+++ b/Source/gg2/Objects/InGameElements/Character.events/Create.xml
@@ -16,6 +16,9 @@
         <argument kind="STRING">{
     player = global.paramPlayer;
     team = player.team;
+    // Created on first use in Begin Step; -1 until then.
+    jumpFlameParticleType = -1;
+    jumpDustParticleType = -1;
     
     left_bound_offset = sprite_get_bbox_left(sprite_index)-sprite_get_xoffset(sprite_index);
     right_bound_offset = sprite_get_bbox_right(sprite_index)-sprite_get_xoffset(sprite_index);
diff --git a/Source/gg2/Objects/InGameElements/Character.events/Destroy.xml b/Source/gg2/Objects/InGameElements/Character.events/Destroy.xml
index 4a869a33..05705538 100644
--- a/Source/gg2/Objects/InGameElements/Character.events/Destroy.xml
+++ b/Source/gg2/Objects/InGameElements/Character.events/Destroy.xml
@@ -61,11 +61,13 @@
     }
     
     //part type destroy
-    if(variable_local_exists("jumpFlameParticleType"))
+    if(jumpFlameParticleType != -1)
         part_type_destroy(jumpFlameParticleType);
     
-    if(variable_local_exists("jumpDustParticleSystem"))
-        part_type_destroy(jumpDustParticleSystem);
+    // Was a check for jumpDustParticleSystem, which never existed locally, so
+    // the dust particle type leaked.
+    if(jumpDustParticleType != -1)
+        part_type_destroy(jumpDustParticleType);
         
     // destroy overlay lists
     ds_list_destroy(stillOverlays);
diff --git a/Source/gg2/Objects/InGameElements/Player.events/Create.xml b/Source/gg2/Objects/InGameElements/Player.events/Create.xml
index 606de443..8d3a9f1c 100644
--- a/Source/gg2/Objects/InGameElements/Player.events/Create.xml
+++ b/Source/gg2/Objects/InGameElements/Player.events/Create.xml
@@ -16,6 +16,18 @@
         <argument kind="STRING">{
     object = -1;
     team = TEAM_SPECTATOR;
+
+    // Server-side command/auth state, used by processClientCommands and rewardAuthStart.
+    // 0: waiting for command byte.
+    // 1: waiting for command data length (1 byte)
+    // 2: waiting for command data.
+    commandReceiveState = 0;
+    commandReceiveExpectedBytes = 1;
+    commandReceiveCommand = 0;
+    lastNamechange = -1; // current_time of the last name change, -1 if none
+    challenge = "";      // reward auth challenge, "" until requested
+    rewardId = "";
+    rewardAuthAlreadyStarted = false;
     class = CLASS_SCOUT;
     socket = -1;
     name = "";
diff --git a/Source/gg2/Objects/InGameElements/Weapon.events/Create.xml b/Source/gg2/Objects/InGameElements/Weapon.events/Create.xml
index 465c8518..f17ca7f8 100644
--- a/Source/gg2/Objects/InGameElements/Weapon.events/Create.xml
+++ b/Source/gg2/Objects/InGameElements/Weapon.events/Create.xml
@@ -13,7 +13,9 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">xoffset += sprite_xoffset;
+        <argument kind="STRING">// Weapons with ammo set this after event_inherited; -1 means none.
+maxAmmo = -1;
+xoffset += sprite_xoffset;
 yoffset += sprite_yoffset;
 readyToShoot = false;
 justShot = false;
diff --git a/Source/gg2/Objects/Map elements/Gamemode-specific/CP/ControlPoint.events/Step.xml b/Source/gg2/Objects/Map elements/Gamemode-specific/CP/ControlPoint.events/Step.xml
index e8b70a28..e4534018 100644
--- a/Source/gg2/Objects/Map elements/Gamemode-specific/CP/ControlPoint.events/Step.xml	
+++ b/Source/gg2/Objects/Map elements/Gamemode-specific/CP/ControlPoint.events/Step.xml	
@@ -79,7 +79,8 @@ if(not arenaRoundEnd) {
             }
             
             if(player.team == TEAM_RED) {
-                if(ubered) {
+                // TODO(enigma): self. needed inside with(); ENIGMA resolves bare names against the outer object
+                if(self.ubered) {
                     other.redPresence = max(1, other.redPresence);
                 } else {
                     other.redPresence = max(2, other.redPresence);
@@ -87,7 +88,8 @@ if(not arenaRoundEnd) {
                 }
                 randomRedCapper = id;
             } else {
-                if(ubered) {
+                // TODO(enigma): self. needed inside with(); ENIGMA resolves bare names against the outer object
+                if(self.ubered) {
                     other.bluePresence = max(1, other.bluePresence);
                 } else {
                     other.bluePresence = max(2, other.bluePresence);
diff --git a/Source/gg2/Objects/Menus/Main Menu Elements/DevMessageChecker.events/Create.xml b/Source/gg2/Objects/Menus/Main Menu Elements/DevMessageChecker.events/Create.xml
index 2dd6cb66..d2a01213 100644
--- a/Source/gg2/Objects/Menus/Main Menu Elements/DevMessageChecker.events/Create.xml	
+++ b/Source/gg2/Objects/Menus/Main Menu Elements/DevMessageChecker.events/Create.xml	
@@ -16,11 +16,9 @@
         <argument kind="STRING">handleCreated = false;
 
 // We only want to check for messages once per run
-if(variable_global_exists("devMessagesChecked")) {
-    if(global.devMessagesChecked) {
-        instance_destroy();
-        exit;
-    }
+if(global.devMessagesChecked) {
+    instance_destroy();
+    exit;
 }
 
 // downloads a text file off the internet, and parses it for instructions
diff --git a/Source/gg2/Objects/Menus/Main Menu Elements/MainMenuController.events/Create.xml b/Source/gg2/Objects/Menus/Main Menu Elements/MainMenuController.events/Create.xml
index 3747adee..2bf6f5d5 100644
--- a/Source/gg2/Objects/Menus/Main Menu Elements/MainMenuController.events/Create.xml	
+++ b/Source/gg2/Objects/Menus/Main Menu Elements/MainMenuController.events/Create.xml	
@@ -70,7 +70,7 @@
         validBackground = false;
 
         // already loaded background in a previous instance
-        if (variable_global_exists("downloadedBackground"))
+        if (global.downloadedBackground != -1)
         {
             menuimage = global.downloadedBackground;
             validBackground = true;
diff --git a/Source/gg2/Objects/Menus/MenuController.events/Create.xml b/Source/gg2/Objects/Menus/MenuController.events/Create.xml
index 6afed82b..801202bd 100644
--- a/Source/gg2/Objects/Menus/MenuController.events/Create.xml
+++ b/Source/gg2/Objects/Menus/MenuController.events/Create.xml
@@ -13,7 +13,12 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">show_error("The Create event needs to be overridden in all child objects.", true);
+        <argument kind="STRING">// TODO(enigma): never runs (children override Create and call menu_create), but
+// ENIGMA needs these assigned in MenuController's own events to declare them
+// for its Draw event.
+dimmed = false;
+bgtabs = false;
+show_error("The Create event needs to be overridden in all child objects.", true);
 </argument>
       </arguments>
     </action>
diff --git a/Source/gg2/Objects/Menus/MenuController.events/Draw.xml b/Source/gg2/Objects/Menus/MenuController.events/Draw.xml
index 0b59e003..8ee17286 100644
--- a/Source/gg2/Objects/Menus/MenuController.events/Draw.xml
+++ b/Source/gg2/Objects/Menus/MenuController.events/Draw.xml
@@ -13,7 +13,7 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">if(!variable_local_exists("baseybegin")) // This needs to be duplicated because of code execution order issues
+        <argument kind="STRING">if(mouse_samples == -1) // This needs to be duplicated because of code execution order issues
 {
     baseybegin = ybegin;
     mouse_samples = ds_list_create();
diff --git a/Source/gg2/Objects/Menus/MenuController.events/Step.xml b/Source/gg2/Objects/Menus/MenuController.events/Step.xml
index 57cba1a4..72bde3e5 100644
--- a/Source/gg2/Objects/Menus/MenuController.events/Step.xml
+++ b/Source/gg2/Objects/Menus/MenuController.events/Step.xml
@@ -13,7 +13,7 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">if(!variable_local_exists("baseybegin"))
+        <argument kind="STRING">if(mouse_samples == -1)
 {
     baseybegin = ybegin;
     mouse_samples = ds_list_create();
diff --git a/Source/gg2/Objects/Networking/Client.events/Create.xml b/Source/gg2/Objects/Networking/Client.events/Create.xml
index e79db868..27156a93 100644
--- a/Source/gg2/Objects/Networking/Client.events/Create.xml
+++ b/Source/gg2/Objects/Networking/Client.events/Create.xml
@@ -1,6 +1,25 @@
 <?xml version="1.0" encoding="UTF-8" standalone="no"?>
 <event category="CREATE" id="0">
   <actions>
+    <action id="603" library="1">
+      <!--action name: Code-->
+      <kind>CODE</kind>
+      <allowRelative>false</allowRelative>
+      <question>false</question>
+      <canApplyTo>true</canApplyTo>
+      <actionType>CODE</actionType>
+      <functionName/>
+      <relative>false</relative>
+      <not>false</not>
+      <appliesTo>.self</appliesTo>
+      <arguments>
+        <argument kind="STRING">// TODO(enigma): ENIGMA only sees instance variables assigned in the object's own
+// events, not in scripts it calls. These are set by ClientCreate/ClientBeginStep.
+nocreate = false;
+downloadingMap = false;
+</argument>
+      </arguments>
+    </action>
     <action id="601" library="1">
       <!--action name: Execute_Script-->
       <kind>NORMAL</kind>
diff --git a/Source/gg2/Objects/Networking/HttpClient.xml b/Source/gg2/Objects/Networking/HttpClient.xml
new file mode 100644
index 00000000..567a1e55
--- /dev/null
+++ b/Source/gg2/Objects/Networking/HttpClient.xml
@@ -0,0 +1,10 @@
+<?xml version="1.0" encoding="UTF-8" standalone="no"?>
+<object id="848">
+  <sprite/>
+  <solid>false</solid>
+  <visible>false</visible>
+  <depth>0</depth>
+  <persistent>true</persistent>
+  <parent/>
+  <mask/>
+</object>
diff --git a/Source/gg2/Objects/Networking/_resources.list.xml b/Source/gg2/Objects/Networking/_resources.list.xml
index 5b2ae838..e941aec0 100644
--- a/Source/gg2/Objects/Networking/_resources.list.xml
+++ b/Source/gg2/Objects/Networking/_resources.list.xml
@@ -3,4 +3,5 @@
   <resource name="GameServer" type="RESOURCE"/>
   <resource name="Client" type="RESOURCE"/>
   <resource name="RewardAuthChecker" type="RESOURCE"/>
+  <resource name="HttpClient" type="RESOURCE"/>
 </resources>
diff --git a/Source/gg2/Objects/Overlays/AmmoCounter.events/Draw.xml b/Source/gg2/Objects/Overlays/AmmoCounter.events/Draw.xml
index 35979af6..2867338f 100644
--- a/Source/gg2/Objects/Overlays/AmmoCounter.events/Draw.xml
+++ b/Source/gg2/Objects/Overlays/AmmoCounter.events/Draw.xml
@@ -79,7 +79,8 @@ if global.myself.object != -1 {
             {
                 var offset, flarecolor;
                 
-                if(readyToFlare)
+                // TODO(enigma): self. needed inside with(); ENIGMA resolves bare names against the outer object
+                if(self.readyToFlare)
                     flarecolor = c_white;
                 else
                     flarecolor = c_gray;
diff --git a/Source/gg2/Objects/Overlays/Gamemodes/ArenaHUD.events/Create.xml b/Source/gg2/Objects/Overlays/Gamemodes/ArenaHUD.events/Create.xml
index 3dd3f3fa..105eb55d 100644
--- a/Source/gg2/Objects/Overlays/Gamemodes/ArenaHUD.events/Create.xml
+++ b/Source/gg2/Objects/Overlays/Gamemodes/ArenaHUD.events/Create.xml
@@ -13,7 +13,11 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">/*
+        <argument kind="STRING">// MVP counts per team; set at round end, 0 until then.
+mvps[TEAM_RED] = 0;
+mvps[TEAM_BLUE] = 0;
+
+/*
 The Arena game mode consists of multiple rounds. Each round starts with a fixed-time setup period where
 players can spawn and change their team and class. This is followed by the round proper, which is won
 by the first team to either eliminate the enemy team or gain control of the ArenaCP, which unlocks a
diff --git a/Source/gg2/Objects/Overlays/Gamemodes/ArenaHUD.events/Draw.xml b/Source/gg2/Objects/Overlays/Gamemodes/ArenaHUD.events/Draw.xml
index 06f73959..60fc9b09 100644
--- a/Source/gg2/Objects/Overlays/Gamemodes/ArenaHUD.events/Draw.xml
+++ b/Source/gg2/Objects/Overlays/Gamemodes/ArenaHUD.events/Draw.xml
@@ -94,7 +94,7 @@ if (state == ARENA_STATE_ROUND_END and global.winners == -1 and !instance_exists
 
     draw_sprite_ext(MVPBannerS, mvpSubimg, xoffset+xsize/2, yoffset+ysize/2, 2, 2, 0, c_white, 0.8);
     
-    if(variable_local_exists("mvps"))
+    if(mvps[mvpTeam] > 0)
     {
         // The mvps don't exist for players who join during round end, since syncing them in the FULL_UPDATE
         // would make things more complicated, and it should happen rarely enough that seeing an empty
diff --git a/Source/gg2/Objects/Overlays/ScoreTableController.events/Draw.xml b/Source/gg2/Objects/Overlays/ScoreTableController.events/Draw.xml
index f6d985ad..6088c5a9 100644
--- a/Source/gg2/Objects/Overlays/ScoreTableController.events/Draw.xml
+++ b/Source/gg2/Objects/Overlays/ScoreTableController.events/Draw.xml
@@ -47,7 +47,8 @@
     
     with(JoiningPlayer)
     {
-        if(occupiesSlot)
+        // TODO(enigma): self. needed inside with(); ENIGMA resolves bare names against the outer object
+        if(self.occupiesSlot)
             ds_priority_add(spec, id, 0);
     }
     
diff --git a/Source/gg2/Objects/Projectiles/BladeB.events/Create.xml b/Source/gg2/Objects/Projectiles/BladeB.events/Create.xml
index ff49c0ea..f40f3416 100644
--- a/Source/gg2/Objects/Projectiles/BladeB.events/Create.xml
+++ b/Source/gg2/Objects/Projectiles/BladeB.events/Create.xml
@@ -13,7 +13,9 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">{
+        <argument kind="STRING">// move_all_bullets clears this after the first step
+firststep = true;
+{
     hitDamage = 5;
     alarm[0] = 15 / global.delta_factor;
     ammoDrained = 0;
diff --git a/Source/gg2/Objects/Projectiles/BurningProjectile.events/Create.xml b/Source/gg2/Objects/Projectiles/BurningProjectile.events/Create.xml
index 95d3e5cd..414996ce 100644
--- a/Source/gg2/Objects/Projectiles/BurningProjectile.events/Create.xml
+++ b/Source/gg2/Objects/Projectiles/BurningProjectile.events/Create.xml
@@ -13,7 +13,9 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">// Child objects will need to define:
+        <argument kind="STRING">// move_all_bullets clears this after the first step
+firststep = true;
+// Child objects will need to define:
 // hitDamage, flameLife, burnIncrease, durationIncrease, afterburnFalloff (bool), penetrateCap
 // Then they need to call the inherited event (i.e. this code)
 alarm[0] = flameLife / global.delta_factor;
diff --git a/Source/gg2/Objects/Projectiles/Mine.events/Create.xml b/Source/gg2/Objects/Projectiles/Mine.events/Create.xml
index c8729136..43da5a37 100644
--- a/Source/gg2/Objects/Projectiles/Mine.events/Create.xml
+++ b/Source/gg2/Objects/Projectiles/Mine.events/Create.xml
@@ -13,7 +13,9 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">{
+        <argument kind="STRING">// move_all_bullets clears this after the first step
+firststep = true;
+{
     explosionDamage = 45;
     animationState = 1;
     alarm[1] = 29 / global.delta_factor;
diff --git a/Source/gg2/Objects/Projectiles/Needle.events/Create.xml b/Source/gg2/Objects/Projectiles/Needle.events/Create.xml
index 3651d213..56fead11 100644
--- a/Source/gg2/Objects/Projectiles/Needle.events/Create.xml
+++ b/Source/gg2/Objects/Projectiles/Needle.events/Create.xml
@@ -13,7 +13,9 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">{
+        <argument kind="STRING">// move_all_bullets clears this after the first step
+firststep = true;
+{
     hitDamage = 4;
     lifetime = 40;
     alarm[0] = lifetime / global.delta_factor;
diff --git a/Source/gg2/Objects/Projectiles/Rocket.events/Create.xml b/Source/gg2/Objects/Projectiles/Rocket.events/Create.xml
index 5b33b938..78ef4050 100644
--- a/Source/gg2/Objects/Projectiles/Rocket.events/Create.xml
+++ b/Source/gg2/Objects/Projectiles/Rocket.events/Create.xml
@@ -13,7 +13,11 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">{
+        <argument kind="STRING">// move_all_bullets clears this after the first step
+firststep = true;
+// Created on first use in move_all_bullets; -1 until then.
+rocketblurParticleType = -1;
+{
     hitDamage = 25;
     explosionDamage = 30;
     blastRadius = 65;
diff --git a/Source/gg2/Objects/Projectiles/Rocket.events/Destroy.xml b/Source/gg2/Objects/Projectiles/Rocket.events/Destroy.xml
index 1b239385..48168071 100644
--- a/Source/gg2/Objects/Projectiles/Rocket.events/Destroy.xml
+++ b/Source/gg2/Objects/Projectiles/Rocket.events/Destroy.xml
@@ -13,7 +13,7 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">if(variable_local_exists("rocketblurParticleType")) {
+        <argument kind="STRING">if(rocketblurParticleType != -1) {
     part_type_destroy(rocketblurParticleType);
 }
 ds_list_destroy(playersPassed);
diff --git a/Source/gg2/Objects/Projectiles/Shot.events/Create.xml b/Source/gg2/Objects/Projectiles/Shot.events/Create.xml
index b50c0843..4966c646 100644
--- a/Source/gg2/Objects/Projectiles/Shot.events/Create.xml
+++ b/Source/gg2/Objects/Projectiles/Shot.events/Create.xml
@@ -13,7 +13,9 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">{
+        <argument kind="STRING">// move_all_bullets clears this after the first step
+firststep = true;
+{
     hitDamage = 8;
     lifetime = 40;
     alarm[0] = lifetime / global.delta_factor;
diff --git a/Source/gg2/Objects/Projectiles/StabAnim.events/Create.xml b/Source/gg2/Objects/Projectiles/StabAnim.events/Create.xml
index 75c3d56b..6bb951e2 100644
--- a/Source/gg2/Objects/Projectiles/StabAnim.events/Create.xml
+++ b/Source/gg2/Objects/Projectiles/StabAnim.events/Create.xml
@@ -13,7 +13,8 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">alpha = 0.01;
+        <argument kind="STRING">golden = false; // set by the creating Revolver
+alpha = 0.01;
 done = false;
 hitDamage = 0;
 image_speed = 0;
diff --git a/Source/gg2/Objects/Weapons/Rifle.events/User Event 3.xml b/Source/gg2/Objects/Weapons/Rifle.events/User Event 3.xml
index 720741ad..041a6f54 100644
--- a/Source/gg2/Objects/Weapons/Rifle.events/User Event 3.xml	
+++ b/Source/gg2/Objects/Weapons/Rifle.events/User Event 3.xml	
@@ -122,7 +122,8 @@ ds_list_destroy(bulletStoppingObjects);
 // Apply dramatic effect
 with(hitInstance) {
     if(object_index == Character or object_is_ancestor(object_index, Character)) {
-        if(!ubered and team != other.owner.team) {
+        // TODO(enigma): self. needed inside with(); ENIGMA resolves bare names against the outer object
+        if(!self.ubered and team != other.owner.team) {
             damageCharacter(other.ownerPlayer, id, other.hitDamage);
             if (lastDamageDealer != other.ownerPlayer and lastDamageDealer != player)
             {
diff --git a/Source/gg2/Objects/Weapons/SentryTurret.events/End Step.xml b/Source/gg2/Objects/Weapons/SentryTurret.events/End Step.xml
index c0443e62..ee058b3d 100644
--- a/Source/gg2/Objects/Weapons/SentryTurret.events/End Step.xml	
+++ b/Source/gg2/Objects/Weapons/SentryTurret.events/End Step.xml	
@@ -13,7 +13,8 @@
       <not>false</not>
       <appliesTo>.self</appliesTo>
       <arguments>
-        <argument kind="STRING">var targetQueue, testDist, playercheck, targetAngle, obscured, rotateoffset;
+        <argument kind="STRING">// rotateoffset is an instance variable (User Event 0), not a local here.
+var targetQueue, testDist, playercheck, targetAngle, obscured;
 
 if(ownerPlayer.sentry)
 {
@@ -43,7 +44,8 @@ targetQueue = ds_priority_create();
 // Build a queue of potential targets
 with(Character) {
     testDist = distance_to_object(other);
-    if(!cloak &amp;&amp; testDist&lt;=375) {
+    // TODO(enigma): self. needed inside with(); ENIGMA resolves bare names against the outer object
+    if(!self.cloak &amp;&amp; testDist&lt;=375) {
         ds_priority_add(targetQueue, id, testDist);
     }
 }
diff --git a/Source/gg2/Rooms/BuilderRoom.xml b/Source/gg2/Rooms/BuilderRoom.xml
index 6385a4cd..ab381d55 100644
--- a/Source/gg2/Rooms/BuilderRoom.xml
+++ b/Source/gg2/Rooms/BuilderRoom.xml
@@ -9,9 +9,10 @@
   <speed>30</speed>
   <persistent>false</persistent>
   <creationCode>with(Builder) {
-    if (visible) event_user(0);
+    // TODO(enigma): self. needed inside with(); ENIGMA resolves bare names against the outer object
+    if (self.visible) event_user(0);
     else {
-        visible = true;
+        self.visible = true;
         resetToggles();
         CustomMapInit("Maps/ggb2_tmp_map.png");
         loadMetadata(Builder.metadata, true);
diff --git a/Source/gg2/Scripts/Bytes/md5_bin.gml b/Source/gg2/Scripts/Bytes/md5_bin.gml
index 63d39f1f..431e7ec1 100644
--- a/Source/gg2/Scripts/Bytes/md5_bin.gml
+++ b/Source/gg2/Scripts/Bytes/md5_bin.gml
@@ -19,8 +19,9 @@
 {
     var str,uint,grp,rol,i,j,h,len,pos,w,a,b,c,d,e,f,temp,digest;
     str = argument0;
-    if (!variable_global_exists("MD5k")) {
+    if (!global.md5TablesReady) {
         globalvar MD5k,MD5g,MD5r,MD5s;
+        global.md5TablesReady = true;
         grp  = "00010203040506070809101112131415";
         grp += "01061100051015040914030813020712";
         grp += "05081114010407101300030609121502";
diff --git a/Source/gg2/Scripts/Damage API/dealDamage.gml b/Source/gg2/Scripts/Damage API/dealDamage.gml
index 424adeba..eb96e1e6 100644
--- a/Source/gg2/Scripts/Damage API/dealDamage.gml	
+++ b/Source/gg2/Scripts/Damage API/dealDamage.gml	
@@ -1,7 +1,8 @@
 // dealDamage( sourcePlayer, damagedObject, damageDealt )
 with(argument1)
 {
-    if(variable_local_exists("deathmatch_invulnerable"))
+    // Only characters have deathmatch_invulnerable (always set in Character Create).
+    if(object_index == Character or object_is_ancestor(object_index, Character))
     {
         if(argument1.deathmatch_invulnerable != 0)
             return 0;
diff --git a/Source/gg2/Scripts/Faucet HTTP/__http_init.gml b/Source/gg2/Scripts/Faucet HTTP/__http_init.gml
index 06f74eec..10ad5753 100644
--- a/Source/gg2/Scripts/Faucet HTTP/__http_init.gml	
+++ b/Source/gg2/Scripts/Faucet HTTP/__http_init.gml	
@@ -20,5 +20,6 @@
 // Creates global.__HttpClient
 // real __http_init()
 
-global.__HttpClient = object_add();
-object_set_persistent(global.__HttpClient, true);
+// HttpClient is a persistent object with no events. GG2: was object_add(),
+// which ENIGMA can't do (objects are compiled in).
+global.__HttpClient = HttpClient;
diff --git a/Source/gg2/Scripts/Faucet HTTP/http_new_get.gml b/Source/gg2/Scripts/Faucet HTTP/http_new_get.gml
index 6d85e187..9b40d21d 100644
--- a/Source/gg2/Scripts/Faucet HTTP/http_new_get.gml	
+++ b/Source/gg2/Scripts/Faucet HTTP/http_new_get.gml	
@@ -30,7 +30,7 @@ var url, client;
 
 url = argument0;
 
-if (!variable_global_exists('__HttpClient'))
+if (global.__HttpClient == -1)
     __http_init();
 
 client = instance_create(0, 0, global.__HttpClient);
diff --git a/Source/gg2/Scripts/Faucet HTTP/http_new_get_ex.gml b/Source/gg2/Scripts/Faucet HTTP/http_new_get_ex.gml
index 8d058a6c..007e92d9 100644
--- a/Source/gg2/Scripts/Faucet HTTP/http_new_get_ex.gml	
+++ b/Source/gg2/Scripts/Faucet HTTP/http_new_get_ex.gml	
@@ -32,7 +32,7 @@ var url, headers, client;
 url = argument0;
 headers = argument1;
 
-if (!variable_global_exists('__HttpClient'))
+if (global.__HttpClient == -1)
     __http_init();
 
 client = instance_create(0, 0, global.__HttpClient);
diff --git a/Source/gg2/Scripts/Faucet HTTP/http_new_post.gml b/Source/gg2/Scripts/Faucet HTTP/http_new_post.gml
index c7f64713..cb9f8936 100644
--- a/Source/gg2/Scripts/Faucet HTTP/http_new_post.gml	
+++ b/Source/gg2/Scripts/Faucet HTTP/http_new_post.gml	
@@ -34,7 +34,7 @@ url = argument0;
 body = argument1;
 mimeType = argument2;
 
-if (!variable_global_exists('__HttpClient'))
+if (global.__HttpClient == -1)
     __http_init();
 
 client = instance_create(0, 0, global.__HttpClient);
diff --git a/Source/gg2/Scripts/Faucet HTTP/http_new_post_ex.gml b/Source/gg2/Scripts/Faucet HTTP/http_new_post_ex.gml
index 6a307f66..f2759a1b 100644
--- a/Source/gg2/Scripts/Faucet HTTP/http_new_post_ex.gml	
+++ b/Source/gg2/Scripts/Faucet HTTP/http_new_post_ex.gml	
@@ -36,7 +36,7 @@ body = argument1;
 mimeType = argument2;
 headers = argument3;
 
-if (!variable_global_exists('__HttpClient'))
+if (global.__HttpClient == -1)
     __http_init();
 
 client = instance_create(0, 0, global.__HttpClient);
diff --git a/Source/gg2/Scripts/Game/game_init.gml b/Source/gg2/Scripts/Game/game_init.gml
index 8572cdc1..ca57e12f 100644
--- a/Source/gg2/Scripts/Game/game_init.gml
+++ b/Source/gg2/Scripts/Game/game_init.gml
@@ -1,5 +1,22 @@
 // Returns true if the game is successfully initialized, false if there was an error and we should quit.
 {
+    // Lazily-created globals start at a sentinel instead of being checked with
+    // variable_global_exists, which ENIGMA doesn't have (variables are compiled in).
+    global.runningPingCount = 0;
+    global.devMessagesChecked = false;
+    global.downloadedBackground = -1;
+    global.md5TablesReady = false;
+    global.gearOverlayInfo = -1;
+    global.headPoseInfo = -1;
+    global.spawnPointsCreated = false;
+    global.jumpFlameParticleSystem = -1;
+    global.jumpDustParticleSystem = -1;
+    global.rocketblurParticleSystem = -1;
+    global.flameParticleType = -1;
+    global.flameParticleSystem = -1;
+    global.__HttpClient = -1;
+    global.spritesByName = -1;
+
     initCharacterSpritePrefixes();
     initAllHeadPoses();
     initGear();
diff --git a/Source/gg2/Scripts/GameServer/processClientCommands.gml b/Source/gg2/Scripts/GameServer/processClientCommands.gml
index 018e1372..8bc6b0b6 100644
--- a/Source/gg2/Scripts/GameServer/processClientCommands.gml
+++ b/Source/gg2/Scripts/GameServer/processClientCommands.gml
@@ -6,16 +6,7 @@ playerId = argument1;
 // To prevent players from flooding the server, limit the number of commands to process per step and player.
 commandLimitRemaining = 10;
 
-with(player) {
-    if(!variable_local_exists("commandReceiveState")) {
-        // 0: waiting for command byte.
-        // 1: waiting for command data length (1 byte)
-        // 2: waiting for command data.
-        commandReceiveState = 0;
-        commandReceiveExpectedBytes = 1;
-        commandReceiveCommand = 0;
-    }
-}
+// Command receive state (commandReceiveState etc.) is initialized in Player Create.
 
 while(commandLimitRemaining > 0) {
     var socket;
@@ -293,7 +284,7 @@ while(commandLimitRemaining > 0) {
             {
                 with(player)
                 {
-                    if(variable_local_exists("lastNamechange")) 
+                    if(lastNamechange != -1)
                         if(current_time - lastNamechange < 1000)
                             break;
                     lastNamechange = current_time;
@@ -333,7 +324,7 @@ while(commandLimitRemaining > 0) {
             answer = read_binstring(socket, 16);
             
             with(player)
-                if(variable_local_exists("challenge") and variable_local_exists("rewardId"))
+                if(challenge != "") // challenge and rewardId are set together
                     rewardAuthStart(player, answer, challenge, true, rewardId);
            
             break;
diff --git a/Source/gg2/Scripts/Gear/setGearOverlayInfo.gml b/Source/gg2/Scripts/Gear/setGearOverlayInfo.gml
index 723a146e..3c2ca086 100644
--- a/Source/gg2/Scripts/Gear/setGearOverlayInfo.gml
+++ b/Source/gg2/Scripts/Gear/setGearOverlayInfo.gml
@@ -15,7 +15,7 @@ xscale = argument8;
 subimageSelectionScript = argument9;
 zindex = argument10;
 
-if(!variable_global_exists("gearOverlayInfo"))
+if(global.gearOverlayInfo == -1)
     global.gearOverlayInfo = ds_map_create();
     
 var spriteMap;
diff --git a/Source/gg2/Scripts/Gear/setHeadPose.gml b/Source/gg2/Scripts/Gear/setHeadPose.gml
index 1d75363e..7dc2a869 100644
--- a/Source/gg2/Scripts/Gear/setHeadPose.gml
+++ b/Source/gg2/Scripts/Gear/setHeadPose.gml
@@ -17,7 +17,7 @@ dy = argument3;
 angle = argument4;
 xscale = argument5;
 
-if(!variable_global_exists("headPoseInfo"))
+if(global.headPoseInfo == -1)
     global.headPoseInfo = ds_map_create();
 
 var subimageMap;
diff --git a/Source/gg2/Scripts/Kill Log/createGlobalDamageSource.gml b/Source/gg2/Scripts/Kill Log/createGlobalDamageSource.gml
index 5f24c137..930fa07e 100644
--- a/Source/gg2/Scripts/Kill Log/createGlobalDamageSource.gml	
+++ b/Source/gg2/Scripts/Kill Log/createGlobalDamageSource.gml	
@@ -1,13 +1,14 @@
-// createGlobalDamageSource( name, sprite )
-// creates a damage source which can be used for the kill log
-// and registers it as a globalvar, so that it can be accessed directly
-// from code just like a constant.
-
-var name, sprite;
-
-name = argument0;
-sprite = argument1;
-
-execute_string("globalvar " + name + ";");
-variable_global_set(name, createDamageSource(sprite));
-
+// PLUGINS(disabled): no runtime GML execution in ENIGMA; revisit
+// // createGlobalDamageSource( name, sprite )
+// // creates a damage source which can be used for the kill log
+// // and registers it as a globalvar, so that it can be accessed directly
+// // from code just like a constant.
+//
+// var name, sprite;
+//
+// name = argument0;
+// sprite = argument1;
+//
+// execute_string("globalvar " + name + ";");
+// variable_global_set(name, createDamageSource(sprite));
+//
diff --git a/Source/gg2/Scripts/Kill Log/initializeDamageSources.gml b/Source/gg2/Scripts/Kill Log/initializeDamageSources.gml
index d4dde7c9..fa3a8f77 100644
--- a/Source/gg2/Scripts/Kill Log/initializeDamageSources.gml	
+++ b/Source/gg2/Scripts/Kill Log/initializeDamageSources.gml	
@@ -1,28 +1,41 @@
 global.damageSourceIcons = ds_list_create();
 
-createGlobalDamageSource("DAMAGE_SOURCE_NEEDLEGUN",           NeedleKL);
-createGlobalDamageSource("DAMAGE_SOURCE_RIFLE",               RifleKL);
-createGlobalDamageSource("DAMAGE_SOURCE_RIFLE_CHARGED",       RifleChargedKL);
-createGlobalDamageSource("DAMAGE_SOURCE_MINEGUN",             MineKL);
-createGlobalDamageSource("DAMAGE_SOURCE_MINIGUN",             MinigunKL);
-createGlobalDamageSource("DAMAGE_SOURCE_FLAMETHROWER",        FlameKL);
-createGlobalDamageSource("DAMAGE_SOURCE_SCATTERGUN",          ScatterKL);
-createGlobalDamageSource("DAMAGE_SOURCE_SHOTGUN",             ShotgunKL);
-createGlobalDamageSource("DAMAGE_SOURCE_ROCKETLAUNCHER",      RocketKL);
-createGlobalDamageSource("DAMAGE_SOURCE_REVOLVER",            RevolverKL);
-createGlobalDamageSource("DAMAGE_SOURCE_SENTRYTURRET",        TurretKL);
-createGlobalDamageSource("DAMAGE_SOURCE_BLADE",               BladeKL);
-createGlobalDamageSource("DAMAGE_SOURCE_BUBBLE",              BubbleKL);
-createGlobalDamageSource("DAMAGE_SOURCE_REFLECTED_ROCKET",    RocketReflectKL);
-createGlobalDamageSource("DAMAGE_SOURCE_REFLECTED_STICKY",    MineReflectKL);
-createGlobalDamageSource("DAMAGE_SOURCE_KNIFE",               KnifeKL);
-createGlobalDamageSource("DAMAGE_SOURCE_BACKSTAB",            BackstabKL);
-createGlobalDamageSource("DAMAGE_SOURCE_FLARE",               FlareKL);
-createGlobalDamageSource("DAMAGE_SOURCE_REFLECTED_FLARE",     FlareReflectKL);
-createGlobalDamageSource("DAMAGE_SOURCE_KILL_BOX",            DeadKL);
-createGlobalDamageSource("DAMAGE_SOURCE_FRAG_BOX",            DeadKL);
-createGlobalDamageSource("DAMAGE_SOURCE_PITFALL",             DeadKL);
-createGlobalDamageSource("DAMAGE_SOURCE_FINISHED_OFF",        DeadKL);
-createGlobalDamageSource("DAMAGE_SOURCE_FINISHED_OFF_GIB",    DeadKL);
-createGlobalDamageSource("DAMAGE_SOURCE_BID_FAREWELL",        DeadKL);
-createGlobalDamageSource("DAMAGE_SOURCE_GENERATOR_EXPLOSION", ExplodeKL);
+// Declared statically: createGlobalDamageSource built these globals from strings
+// with execute_string, which ENIGMA can't run. The order sets the IDs sent over
+// the network, so keep it.
+globalvar DAMAGE_SOURCE_NEEDLEGUN, DAMAGE_SOURCE_RIFLE, DAMAGE_SOURCE_RIFLE_CHARGED,
+    DAMAGE_SOURCE_MINEGUN, DAMAGE_SOURCE_MINIGUN, DAMAGE_SOURCE_FLAMETHROWER,
+    DAMAGE_SOURCE_SCATTERGUN, DAMAGE_SOURCE_SHOTGUN, DAMAGE_SOURCE_ROCKETLAUNCHER,
+    DAMAGE_SOURCE_REVOLVER, DAMAGE_SOURCE_SENTRYTURRET, DAMAGE_SOURCE_BLADE, DAMAGE_SOURCE_BUBBLE,
+    DAMAGE_SOURCE_REFLECTED_ROCKET, DAMAGE_SOURCE_REFLECTED_STICKY, DAMAGE_SOURCE_KNIFE,
+    DAMAGE_SOURCE_BACKSTAB, DAMAGE_SOURCE_FLARE, DAMAGE_SOURCE_REFLECTED_FLARE,
+    DAMAGE_SOURCE_KILL_BOX, DAMAGE_SOURCE_FRAG_BOX, DAMAGE_SOURCE_PITFALL,
+    DAMAGE_SOURCE_FINISHED_OFF, DAMAGE_SOURCE_FINISHED_OFF_GIB, DAMAGE_SOURCE_BID_FAREWELL,
+    DAMAGE_SOURCE_GENERATOR_EXPLOSION;
+
+DAMAGE_SOURCE_NEEDLEGUN =           createDamageSource(NeedleKL);
+DAMAGE_SOURCE_RIFLE =               createDamageSource(RifleKL);
+DAMAGE_SOURCE_RIFLE_CHARGED =       createDamageSource(RifleChargedKL);
+DAMAGE_SOURCE_MINEGUN =             createDamageSource(MineKL);
+DAMAGE_SOURCE_MINIGUN =             createDamageSource(MinigunKL);
+DAMAGE_SOURCE_FLAMETHROWER =        createDamageSource(FlameKL);
+DAMAGE_SOURCE_SCATTERGUN =          createDamageSource(ScatterKL);
+DAMAGE_SOURCE_SHOTGUN =             createDamageSource(ShotgunKL);
+DAMAGE_SOURCE_ROCKETLAUNCHER =      createDamageSource(RocketKL);
+DAMAGE_SOURCE_REVOLVER =            createDamageSource(RevolverKL);
+DAMAGE_SOURCE_SENTRYTURRET =        createDamageSource(TurretKL);
+DAMAGE_SOURCE_BLADE =               createDamageSource(BladeKL);
+DAMAGE_SOURCE_BUBBLE =              createDamageSource(BubbleKL);
+DAMAGE_SOURCE_REFLECTED_ROCKET =    createDamageSource(RocketReflectKL);
+DAMAGE_SOURCE_REFLECTED_STICKY =    createDamageSource(MineReflectKL);
+DAMAGE_SOURCE_KNIFE =               createDamageSource(KnifeKL);
+DAMAGE_SOURCE_BACKSTAB =            createDamageSource(BackstabKL);
+DAMAGE_SOURCE_FLARE =               createDamageSource(FlareKL);
+DAMAGE_SOURCE_REFLECTED_FLARE =     createDamageSource(FlareReflectKL);
+DAMAGE_SOURCE_KILL_BOX =            createDamageSource(DeadKL);
+DAMAGE_SOURCE_FRAG_BOX =            createDamageSource(DeadKL);
+DAMAGE_SOURCE_PITFALL =             createDamageSource(DeadKL);
+DAMAGE_SOURCE_FINISHED_OFF =        createDamageSource(DeadKL);
+DAMAGE_SOURCE_FINISHED_OFF_GIB =    createDamageSource(DeadKL);
+DAMAGE_SOURCE_BID_FAREWELL =        createDamageSource(DeadKL);
+DAMAGE_SOURCE_GENERATOR_EXPLOSION = createDamageSource(ExplodeKL);
diff --git a/Source/gg2/Scripts/Menus/menu_create.gml b/Source/gg2/Scripts/Menus/menu_create.gml
index 640bb629..afc322d0 100644
--- a/Source/gg2/Scripts/Menus/menu_create.gml
+++ b/Source/gg2/Scripts/Menus/menu_create.gml
@@ -26,6 +26,9 @@ getsHighlight = true;
 bgtabs = false;
 menumode = false;
 screenheight = 600;
+// baseybegin and mouse_samples are set on the first Step/Draw, after the
+// menu's Create has settled ybegin. -1 until then.
+mouse_samples = -1;
 
 // Hack: The back button should usually appear last in the menu, even if plugins
 // add new items later, so we treat it separately.
diff --git a/Source/gg2/Scripts/Misc/_resources.list.xml b/Source/gg2/Scripts/Misc/_resources.list.xml
index 1209530c..89ba0abc 100644
--- a/Source/gg2/Scripts/Misc/_resources.list.xml
+++ b/Source/gg2/Scripts/Misc/_resources.list.xml
@@ -12,5 +12,5 @@
   <resource name="getCharacterSpriteId" type="RESOURCE"/>
   <resource name="format_timer_value" type="RESOURCE"/>
   <resource name="getRelativePathIfDescendant" type="RESOURCE"/>
-  <resource name="asset_get_index" type="RESOURCE"/>
+  <resource name="sprite_find_by_name" type="RESOURCE"/>
 </resources>
diff --git a/Source/gg2/Scripts/Misc/asset_get_index.gml b/Source/gg2/Scripts/Misc/asset_get_index.gml
deleted file mode 100644
index e6c08e20..00000000
--- a/Source/gg2/Scripts/Misc/asset_get_index.gml
+++ /dev/null
@@ -1,15 +0,0 @@
-// Returns the unique identifying index for a game asset from its name (aka the actual resource)
-// You should always make sure that an asset with that name exists, otherwise it will crash.
-// argument0: The name of the game asset to get the index of (a string)
-
-// Check for possible malformed string or code injection
-// Only allow _ as special character
-var checkstr;
-checkstr = string_replace_all(argument0, "_", "");
-if (checkstr != string_lettersdigits(argument0)) {
-    show_error("Invalid asset name [" + argument0 + "] for asset_get_index", false);
-    exit;
-}
-
-return execute_string("return " + argument0);
-
diff --git a/Source/gg2/Scripts/Misc/getCharacterSpriteId.gml b/Source/gg2/Scripts/Misc/getCharacterSpriteId.gml
index 873bdf5c..967293dc 100644
--- a/Source/gg2/Scripts/Misc/getCharacterSpriteId.gml
+++ b/Source/gg2/Scripts/Misc/getCharacterSpriteId.gml
@@ -20,4 +20,4 @@ if(!is_string(team))
         show_error("Attempted to get a sprite for unknown team ID: " + string(team), true);
 }
 
-return asset_get_index(class + team + animation + "S");
+return sprite_find_by_name(class + team + animation + "S");
diff --git a/Source/gg2/Scripts/Misc/sprite_find_by_name.gml b/Source/gg2/Scripts/Misc/sprite_find_by_name.gml
new file mode 100644
index 00000000..603a7b69
--- /dev/null
+++ b/Source/gg2/Scripts/Misc/sprite_find_by_name.gml
@@ -0,0 +1,21 @@
+// Returns the sprite with the given name. Errors if there is none.
+// argument0: sprite name
+//
+// Replaces asset_get_index, which evaluated the name with execute_string
+// (not available in ENIGMA). The name map is built on first use.
+var sprite;
+if (global.spritesByName == -1)
+{
+    var i;
+    global.spritesByName = ds_map_create();
+    // Sprite IDs are small and dense (707 sprites); scan well past the end.
+    for (i = 0; i < 4096; i += 1)
+        if (sprite_exists(i))
+            ds_map_add(global.spritesByName, sprite_get_name(i), i);
+}
+if (!ds_map_exists(global.spritesByName, argument0))
+{
+    show_error("No sprite named [" + argument0 + "]", false);
+    return -1;
+}
+return ds_map_find_value(global.spritesByName, argument0);
diff --git a/Source/gg2/Scripts/Physics/System/move_all_bullets.gml b/Source/gg2/Scripts/Physics/System/move_all_bullets.gml
index 84965299..b9cf314a 100644
--- a/Source/gg2/Scripts/Physics/System/move_all_bullets.gml
+++ b/Source/gg2/Scripts/Physics/System/move_all_bullets.gml
@@ -2,8 +2,6 @@ var imp, colliding, dx, dy, ddx, ddy, mdone, mdist, motion, mdir;
 gunSetSolids();
 with(Shot)
 {
-    if(!variable_local_exists("firststep"))
-        firststep = true;
     
     vspeed += 0.15 * global.delta_factor;
     if(global.particles != PARTICLES_OFF)
@@ -37,8 +35,6 @@ with(Shot)
 }
 with(Rocket)
 {
-    if(!variable_local_exists("firststep"))
-        firststep = true;
     
     if(instance_exists(owner))
     {
@@ -68,7 +64,7 @@ with(Rocket)
                 effect_create_below(ef_smoke,x-hspeed*1.3,y-vspeed*1.3,0,c_gray);
             else if(global.particles == PARTICLES_ALTERNATIVE)
             {
-                if (!variable_local_exists("rocketblurParticleType"))
+                if (rocketblurParticleType == -1)
                 {
                     rocketblurParticleType = part_type_create();
                     if team == TEAM_RED rocketParticleSprite = RedRocketS;
@@ -81,7 +77,7 @@ with(Rocket)
                     part_type_life(rocketblurParticleType, rocketpartlife, rocketpartlife);
                 }
                 
-                if (!variable_global_exists("rocketblurParticleSystem"))
+                if (global.rocketblurParticleSystem == -1)
                 {
                     global.rocketblurParticleSystem = part_system_create();
                     part_system_depth(global.rocketblurParticleSystem, 10);
@@ -125,8 +121,6 @@ with(Rocket)
 }
 with(BladeB)
 {
-    if(!variable_local_exists("firststep"))
-        firststep = true;
 
     colliding = false;
     if(firststep)
@@ -152,13 +146,13 @@ with(BladeB)
 
 if(global.particles == PARTICLES_ALTERNATIVE)
 {
-    if(not variable_global_exists("flameParticleType"))
+    if(global.flameParticleType == -1)
     {
         global.flameParticleType = part_type_create();
         part_type_sprite(global.flameParticleType, FlameS, true, false, true);
         part_type_alpha2(global.flameParticleType, 1, 0.3);
     }
-    if(not variable_global_exists("flameParticleSystem"))
+    if(global.flameParticleSystem == -1)
     {
         global.flameParticleSystem = part_system_create();
         part_system_depth(global.flameParticleSystem, 10);
@@ -167,8 +161,6 @@ if(global.particles == PARTICLES_ALTERNATIVE)
 }
 with(BurningProjectile)
 {
-    if(!variable_local_exists("firststep"))
-        firststep = true;
     
     if(object_index == Flame)
         vspeed += 0.15 * global.delta_factor;
@@ -227,8 +219,6 @@ with(BurningProjectile)
 }
 with(Mine)
 {
-    if(!variable_local_exists("firststep"))
-        firststep = true;
     
     if(stickied)
     {
@@ -308,8 +298,6 @@ with(Mine)
 }
 with(Needle)
 {
-    if(!variable_local_exists("firststep"))
-        firststep = true;
     
     vspeed += 0.2 * global.delta_factor;
     if(global.particles != PARTICLES_OFF)
diff --git a/Source/gg2/Scripts/Rewards/rewardAuthStart.gml b/Source/gg2/Scripts/Rewards/rewardAuthStart.gml
index 1bd808a6..371cb33d 100644
--- a/Source/gg2/Scripts/Rewards/rewardAuthStart.gml
+++ b/Source/gg2/Scripts/Rewards/rewardAuthStart.gml
@@ -7,7 +7,7 @@ rewardId = argument4;
 
 // Prevent one player from requesting auth several times
 with(player)
-    if(variable_local_exists("rewardAuthAlreadyStarted")) exit;
+    if(rewardAuthAlreadyStarted) exit;
 player.rewardAuthAlreadyStarted = true;
 
 if(!instance_exists(RewardAuthChecker))
diff --git a/Source/gg2/Scripts/Spawning/offloadSpawnPoints.gml b/Source/gg2/Scripts/Spawning/offloadSpawnPoints.gml
index 3e44fb9a..36b86819 100644
--- a/Source/gg2/Scripts/Spawning/offloadSpawnPoints.gml
+++ b/Source/gg2/Scripts/Spawning/offloadSpawnPoints.gml
@@ -1,5 +1,5 @@
 var i;
-if(variable_global_exists("spawnPointsRed")) {
+if(global.spawnPointsCreated) {
     for (i=0;i<=4; i+=1){
         ds_list_clear(global.spawnPointsRed[0,i]);
         ds_list_clear(global.spawnPointsRed[1,i]);
@@ -12,7 +12,7 @@ if(variable_global_exists("spawnPointsRed")) {
     }
 }
 
-if(variable_global_exists("spawnPointsBlue")) {
+if(global.spawnPointsCreated) {
     for (i=0;i<=4;i+=1){
         ds_list_clear(global.spawnPointsBlue[0,i]);
         ds_list_clear(global.spawnPointsBlue[1,i]);
@@ -24,6 +24,7 @@ if(variable_global_exists("spawnPointsBlue")) {
         global.spawnPointsBlue[1,i] = ds_list_create();
     }
 }
+global.spawnPointsCreated = true;
 with (SpawnPointRed) {
     ds_list_add(global.spawnPointsRed[0,group],x);
     ds_list_add(global.spawnPointsRed[1,group],y);
