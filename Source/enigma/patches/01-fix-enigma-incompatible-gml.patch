build(enigma): add prebuild and fix ENIGMA-incompatible GML

diff --git a/Source/gg2/Constants.xml b/Source/gg2/Constants.xml
index 5b1cf8d9..e1c5d88d 100644
--- a/Source/gg2/Constants.xml
+++ b/Source/gg2/Constants.xml
@@ -137,4 +137,14 @@
   <constant name="GEAR_NAVIGATORHAT" value="&quot;NavigatorHat&quot;"/>
   <constant name="GEAR_RAYBANN_CAPE" value="&quot;RaybannCape&quot;"/>
   <constant name="GEAR_HAXXY_CAPE" value="&quot;HaxxyCape&quot;"/>
+  <constant name="commandBytesInvalidCommand" value="-1"/>
+  <constant name="commandBytesPrefixLength1" value="-2"/>
+  <constant name="commandBytesPrefixLength2" value="-3"/>
+  <constant name="STATE_EXPECT_HELLO" value="1"/>
+  <constant name="STATE_EXPECT_MESSAGELEN" value="2"/>
+  <constant name="STATE_EXPECT_NAME" value="3"/>
+  <constant name="STATE_EXPECT_PASSWORD" value="4"/>
+  <constant name="STATE_CLIENT_AUTHENTICATED" value="5"/>
+  <constant name="STATE_EXPECT_COMMAND" value="6"/>
+  <constant name="STATE_CLIENT_DOWNLOADING" value="7"/>
 </constants>
diff --git a/Source/gg2/Objects/Alternative Objects/2013 Halloween Mini-Haxxy Rewards/Ghost.events/Step.xml b/Source/gg2/Objects/Alternative Objects/2013 Halloween Mini-Haxxy Rewards/Ghost.events/Step.xml
index b66464e5..71a18c16 100644
--- a/Source/gg2/Objects/Alternative Objects/2013 Halloween Mini-Haxxy Rewards/Ghost.events/Step.xml	
+++ b/Source/gg2/Objects/Alternative Objects/2013 Halloween Mini-Haxxy Rewards/Ghost.events/Step.xml	
@@ -18,7 +18,10 @@
 // Die and fly towards owner
 if (owner.object != -1) {
     sprite_index = GhostDeath;
-    move_towards_point(owner.object.x, owner.object.y, 10);
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var ownerObject;
+    ownerObject = owner.object;
+    move_towards_point(ownerObject.x, ownerObject.y, 10);
 // Chase other players but don't stray far from corpse's position
 } else {
     var hitBoundary;
diff --git a/Source/gg2/Objects/Characters/Spy.events/End Step.xml b/Source/gg2/Objects/Characters/Spy.events/End Step.xml
index 67a2f8cd..8a3639a3 100644
--- a/Source/gg2/Objects/Characters/Spy.events/End Step.xml	
+++ b/Source/gg2/Objects/Characters/Spy.events/End Step.xml	
@@ -19,8 +19,11 @@ if (bubble.alarm[0] &gt; 0)
 
 if (global.myself != -1) if (global.myself.object != -1)
 {
-    if (team != global.myself.object.team
-        and sign(x - global.myself.object.x) == -sign(global.myself.object.image_xscale)
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var myObject;
+    myObject = global.myself.object;
+    if (team != myObject.team
+        and sign(x - myObject.x) == -sign(myObject.image_xscale)
         and !intel
         and global.winners == -1)
     {
diff --git a/Source/gg2/Objects/InGameElements/JoiningPlayer.events/Create.xml b/Source/gg2/Objects/InGameElements/JoiningPlayer.events/Create.xml
index 26cdb9fa..3b2079f7 100644
--- a/Source/gg2/Objects/InGameElements/JoiningPlayer.events/Create.xml
+++ b/Source/gg2/Objects/InGameElements/JoiningPlayer.events/Create.xml
@@ -18,13 +18,9 @@ mapDownloadBuffer = -1;
 occupiesSlot = false;
 kicked = false;
 
-STATE_EXPECT_HELLO = 1; // Hello message: 17 bytes (HELLO+UUID)
-STATE_EXPECT_MESSAGELEN = 2; // 1 byte Message length header 
-STATE_EXPECT_NAME = 3;
-STATE_EXPECT_PASSWORD = 4;
-STATE_CLIENT_AUTHENTICATED = 5;
-STATE_EXPECT_COMMAND = 6;
-STATE_CLIENT_DOWNLOADING = 7;
+// STATE_* are constants (Constants.xml): ENIGMA needs compile-time case labels.
+// STATE_EXPECT_HELLO: Hello message, 17 bytes (HELLO+UUID)
+// STATE_EXPECT_MESSAGELEN: 1 byte message length header
 
 state = STATE_EXPECT_HELLO;
 expectedBytes = 17;
diff --git a/Source/gg2/Objects/InGameElements/Spectator.events/Draw.xml b/Source/gg2/Objects/InGameElements/Spectator.events/Draw.xml
index 3f19bf3c..110e8311 100644
--- a/Source/gg2/Objects/InGameElements/Spectator.events/Draw.xml
+++ b/Source/gg2/Objects/InGameElements/Spectator.events/Draw.xml
@@ -42,20 +42,26 @@ if (tracking)
         draw_sprite_part_ext(SpectatorHealthFill,0,0,(17-hppixels)+10,43,hppixels+1,xoffset+icon_xpos, yoffset+icon_ypos+(17-hppixels)*2,2,2,c_red,1);
         draw_sprite_part_ext(SpectatorHealthFill,0,0,(17-hppixels)+10,43,hppixels+1,xoffset+icon_xpos, yoffset+icon_ypos+(17-hppixels)*2,2,2,c_green,player.object.hp/player.object.maxHp);
     }
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var playerObject;
+    playerObject = player.object;
     draw_sprite(SpectatorCrosshairS, player.team,
-                round(player.object.x) + cos(degtorad(player.object.aimDirection))*player.object.aimDistance,
-                round(player.object.y) - sin(degtorad(player.object.aimDirection))*player.object.aimDistance);
+                round(playerObject.x) + cos(degtorad(playerObject.aimDirection))*playerObject.aimDistance,
+                round(playerObject.y) - sin(degtorad(playerObject.aimDirection))*playerObject.aimDistance);
     
 }
 
 if (global.myself != -1)
 {
-    if ((global.myself.alarm[5] &gt; 0) and (global.myself.team != TEAM_SPECTATOR))
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var myPlayer;
+    myPlayer = global.myself;
+    if ((myPlayer.alarm[5] &gt; 0) and (myPlayer.team != TEAM_SPECTATOR))
     {
         draw_set_valign(fa_center);
         draw_set_halign(fa_left);
         if (!instance_exists(ArenaHUD))
-            draw_text(xoffset+10, yoffset+10,'Respawn in ' +string(ceil(global.myself.alarm[5]/30*global.delta_factor))+ ' second(s).')
+            draw_text(xoffset+10, yoffset+10,'Respawn in ' +string(ceil(myPlayer.alarm[5]/30*global.delta_factor))+ ' second(s).')
         else
             draw_text(xoffset+10, yoffset+10,'No Respawning in Arena');
     }
diff --git a/Source/gg2/Objects/InGameElements/Spectator.events/End Step.xml b/Source/gg2/Objects/InGameElements/Spectator.events/End Step.xml
index 2030b55e..7575bb95 100644
--- a/Source/gg2/Objects/InGameElements/Spectator.events/End Step.xml	
+++ b/Source/gg2/Objects/InGameElements/Spectator.events/End Step.xml	
@@ -68,8 +68,11 @@ else if(tracking)
             {
                 if(!(player.object.cloak &amp;&amp; player.team != myTeam))
                 {
-                    x = round(player.object.x);
-                    y = round(player.object.y);
+                    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+                    var playerObject;
+                    playerObject = player.object;
+                    x = round(playerObject.x);
+                    y = round(playerObject.y);
                 }
                 else
                     stopTracking = true;
@@ -79,8 +82,11 @@ else if(tracking)
             stopTracking = true;
         if (instance_exists(player.corpse) and !player.object and tracking)
         {
-            x = round(player.corpse.x);
-            y = round(player.corpse.y);
+            // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+            var playerCorpse;
+            playerCorpse = player.corpse;
+            x = round(playerCorpse.x);
+            y = round(playerCorpse.y);
         }
     }
     else
diff --git a/Source/gg2/Objects/Map elements/Gamemode-specific/CP/ControlPoint.events/Step.xml b/Source/gg2/Objects/Map elements/Gamemode-specific/CP/ControlPoint.events/Step.xml
index 96e9cc69..e8b70a28 100644
--- a/Source/gg2/Objects/Map elements/Gamemode-specific/CP/ControlPoint.events/Step.xml	
+++ b/Source/gg2/Objects/Map elements/Gamemode-specific/CP/ControlPoint.events/Step.xml	
@@ -142,8 +142,11 @@ else
 
 // If the player stands on the point all the point sounds should be centered for him.
 if(myselfOnPoint) {
-    soundx = global.myself.object.x;
-    soundy = global.myself.object.y;
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var myObject;
+    myObject = global.myself.object;
+    soundx = myObject.x;
+    soundy = myObject.y;
 } else {
     soundx = x;
     soundy = y;
diff --git a/Source/gg2/Objects/Menus/Main Menu Elements/DevMessageChecker.events/Step.xml b/Source/gg2/Objects/Menus/Main Menu Elements/DevMessageChecker.events/Step.xml
index a545aaa7..ba16a7ac 100644
--- a/Source/gg2/Objects/Menus/Main Menu Elements/DevMessageChecker.events/Step.xml	
+++ b/Source/gg2/Objects/Menus/Main Menu Elements/DevMessageChecker.events/Step.xml	
@@ -35,14 +35,17 @@ if (http_step(handle)) {
     var c;
     for(c = 0; c &lt; ds_list_size(data); c += 2)
     {
-        switch(ds_list_find_value(data, c))
+        // TODO(enigma): if/else instead of switch; ENIGMA can't switch on strings. Restore switch when fixed
+        var messageType;
+        messageType = ds_list_find_value(data, c);
+        if (messageType == "ShowMessage")
         {
-        case "ShowMessage":
             var popup;
             popup = instance_create(250, 471, DevMessagePopup);
             popup.msg = ds_list_find_value(data, c + 1);
-            break;
-        case "Version":
+        }
+        else if (messageType == "Version")
+        {
             versioncheck = ds_list_find_value(data, c + 1);
             versionend = string_pos("!",versioncheck);
             version = string_copy(versioncheck,1,versionend-1);
@@ -55,8 +58,9 @@ if (http_step(handle)) {
                 popup.msg = "Updates have been made to Gang Garrison 2!##"+changes;
                 popup.updating = true;
             }
-            break;
-        case "Background":
+        }
+        else if (messageType == "Background")
+        {
             var text, list, hash, title, url, showVersion;
             text = ds_list_find_value(data, c + 1);
             list = ds_list_create();
@@ -131,7 +135,6 @@ if (http_step(handle)) {
                 ini_write_real("Background", "BackgroundShowVersion", global.backgroundShowVersion);
                 ini_close();
             }
-            break;
         }
     }
 
diff --git a/Source/gg2/Objects/Menus/MenuController.events/Draw.xml b/Source/gg2/Objects/Menus/MenuController.events/Draw.xml
index 77bc6012..0b59e003 100644
--- a/Source/gg2/Objects/Menus/MenuController.events/Draw.xml
+++ b/Source/gg2/Objects/Menus/MenuController.events/Draw.xml
@@ -110,35 +110,21 @@ if(view_hview[0] &lt; 600)
 
         draw_text(view_xview[0]+xbegin,view_yview[0]+ybegin+i*spacing,item_name[i]);
         
-        switch (item_type[i])
-        {
-        case "script":
+        // TODO(enigma): if/else instead of switch; ENIGMA can't switch on strings. Restore switch when fixed
+        var itemType;
+        itemType = item_type[i];
+        if (itemType == "script")
             val = "";
-            break;
-        
-        case "editscript":
-        case "edittext2":
-        case "edittext":
-        case "editnum":
+        else if (itemType == "editscript" or itemType == "edittext2" or itemType == "edittext" or itemType == "editnum")
             val = item_value[i];
-            break;
-        
-        case "editselect":
+        else if (itemType == "editselect")
             val = item_option_label[i, item_value[i]];
-            break;
-        
-        case "editkey":
-            if (editing == i)
-            {
-                val = "Please press a key";
-                break;
-            }
-        case "editkeyormouse":
-            if (editing == i)
-            {
-                val = "Please press a key or mouse button";
-                break;
-            }
+        else if (itemType == "editkey" and editing == i)
+            val = "Please press a key";
+        else if ((itemType == "editkey" or itemType == "editkeyormouse") and editing == i)
+            val = "Please press a key or mouse button";
+        else if (itemType == "editkey" or itemType == "editkeyormouse")
+        {
             switch (menu_get_var(i))
             {
             case vk_alt:
@@ -198,7 +184,6 @@ if(view_hview[0] &lt; 600)
                 else val = chr(menu_get_var(i));
                 break;
             }
-            break;
         }
         draw_text(view_xview[0]+xbegin+valueoffset,view_yview[0]+ybegin+i*spacing,sanitiseNewlines(val));
     }
diff --git a/Source/gg2/Objects/Menus/MenuController.events/Key enter pressed.xml b/Source/gg2/Objects/Menus/MenuController.events/Key enter pressed.xml
index 7f53d137..37be179f 100644
--- a/Source/gg2/Objects/Menus/MenuController.events/Key enter pressed.xml	
+++ b/Source/gg2/Objects/Menus/MenuController.events/Key enter pressed.xml	
@@ -14,24 +14,25 @@
       <appliesTo>.self</appliesTo>
       <arguments>
         <argument kind="STRING">if(editing != -1) { 
-    switch (item_type[editing]) {
-        case "edittext":
-            menu_set_var(editing, item_value[editing]);
-            execute_string(item_script[editing], item_value[editing]);
-            break;
-        case "edittext2":
-            item_value[editing] = execute_string(item_script[editing], item_value[editing]);
-            menu_set_var(editing, item_value[editing]);
-            break;
-        case "editnum":
-            var newval;
-            newval = real(string_digits(item_value[editing]));
-            if(item_limit[editing]&gt;0)
-                newval = min(newval, item_limit[editing]);
-            menu_set_var(editing, newval);
-            execute_string(item_script[editing], menu_get_var(editing));
-            item_value[editing] = string(menu_get_var(editing));
-            break;
+    // TODO(enigma): if/else instead of switch; ENIGMA can't switch on strings. Restore switch when fixed
+    var editingType;
+    editingType = item_type[editing];
+    if (editingType == "edittext") {
+        menu_set_var(editing, item_value[editing]);
+        execute_string(item_script[editing], item_value[editing]);
+    }
+    else if (editingType == "edittext2") {
+        item_value[editing] = execute_string(item_script[editing], item_value[editing]);
+        menu_set_var(editing, item_value[editing]);
+    }
+    else if (editingType == "editnum") {
+        var newval;
+        newval = real(string_digits(item_value[editing]));
+        if(item_limit[editing]&gt;0)
+            newval = min(newval, item_limit[editing]);
+        menu_set_var(editing, newval);
+        execute_string(item_script[editing], menu_get_var(editing));
+        item_value[editing] = string(menu_get_var(editing));
     }
     editing = -1;
 }
diff --git a/Source/gg2/Objects/Menus/MenuController.events/Step.xml b/Source/gg2/Objects/Menus/MenuController.events/Step.xml
index a9a6f9a0..57cba1a4 100644
--- a/Source/gg2/Objects/Menus/MenuController.events/Step.xml
+++ b/Source/gg2/Objects/Menus/MenuController.events/Step.xml
@@ -40,17 +40,19 @@ menu_hack_backbutton();
 itemcount = menu_get_itemcount();
 
 if(editing != -1) {
-    switch (item_type[editing]) {
-        case "edittext2":
-        case "edittext":
-        case "editnum":
-            if (keyboard_check(vk_control) &amp;&amp; keyboard_check(ord("V"))) {
-                keyboard_string += clipboard_get_text();
-                keyboard_clear(ord("V"));
-            }
-            item_value[editing] = keyboard_string;
-            break;
-        case "editkeyormouse":
+    // TODO(enigma): if/else instead of switch; ENIGMA can't switch on strings. Restore switch when fixed
+    var editingType;
+    editingType = item_type[editing];
+    if (editingType == "edittext2" or editingType == "edittext" or editingType == "editnum") {
+        if (keyboard_check(vk_control) &amp;&amp; keyboard_check(ord("V"))) {
+            keyboard_string += clipboard_get_text();
+            keyboard_clear(ord("V"));
+        }
+        item_value[editing] = keyboard_string;
+    }
+    else if (editingType == "editkeyormouse" or editingType == "editkey") {
+        // editkeyormouse checks the mouse first, then falls through to the key check
+        if (editingType == "editkeyormouse") {
             if mouse_check_button(mb_left) {
                 menu_set_var(editing, MOUSE_LEFT);
                 execute_string(item_script[editing], MOUSE_LEFT);
@@ -63,14 +65,14 @@ if(editing != -1) {
                 editing = -1;
                 exit;
             }
-        case "editkey":
-            if keyboard_check_direct(vk_tab) keyboard_lastkey = vk_tab;
-            if keyboard_lastkey != 0 {
-                menu_set_var(editing, keyboard_lastkey);
-                execute_string(item_script[editing], menu_get_var(editing));
-                editing = -1;
-                exit;
-            }
+        }
+        if keyboard_check_direct(vk_tab) keyboard_lastkey = vk_tab;
+        if keyboard_lastkey != 0 {
+            menu_set_var(editing, keyboard_lastkey);
+            execute_string(item_script[editing], menu_get_var(editing));
+            editing = -1;
+            exit;
+        }
     }
 } else if(mouse_x &gt; view_xview[0]+xbegin) {
     if(mouse_y &gt;= view_yview[0]+ybegin-(spacing/2) and mouse_x &lt; view_xview[0]+xbegin+width)
@@ -79,29 +81,25 @@ if(editing != -1) {
         if(mouse_check_button_pressed(mb_left)){
             mouse_clear(mb_left);
             if(virtualitem &lt; itemcount) {
-                switch (item_type[virtualitem]) {
-                    case "script":
-                        execute_string(item_script[virtualitem]);
-                        break;
-                    case "editscript":
-                        item_value[virtualitem] = execute_string(item_script[virtualitem]);
-                        break;
-                    case "editkey":
-                    case "editkeyormouse":
-                        io_clear();
-                        editing = virtualitem;
-                        break;
-                    case "edittext":
-                    case "edittext2":
-                    case "editnum":
-                        keyboard_string = "";
-                        editing = virtualitem;
-                        break;
-                    case "editselect":
-                        item_value[virtualitem] = (item_value[virtualitem]+1) mod item_options[virtualitem];
-                        menu_set_var(virtualitem, item_option_value[virtualitem, item_value[virtualitem]]);
-                        execute_string(item_script[virtualitem], menu_get_var(virtualitem));
-                        break;
+                // TODO(enigma): if/else instead of switch; ENIGMA can't switch on strings. Restore switch when fixed
+                var clickedType;
+                clickedType = item_type[virtualitem];
+                if (clickedType == "script")
+                    execute_string(item_script[virtualitem]);
+                else if (clickedType == "editscript")
+                    item_value[virtualitem] = execute_string(item_script[virtualitem]);
+                else if (clickedType == "editkey" or clickedType == "editkeyormouse") {
+                    io_clear();
+                    editing = virtualitem;
+                }
+                else if (clickedType == "edittext" or clickedType == "edittext2" or clickedType == "editnum") {
+                    keyboard_string = "";
+                    editing = virtualitem;
+                }
+                else if (clickedType == "editselect") {
+                    item_value[virtualitem] = (item_value[virtualitem]+1) mod item_options[virtualitem];
+                    menu_set_var(virtualitem, item_option_value[virtualitem, item_value[virtualitem]]);
+                    execute_string(item_script[virtualitem], menu_get_var(virtualitem));
                 }
             }
         }
diff --git a/Source/gg2/Objects/Overlays/AmmoCounter.events/Draw.xml b/Source/gg2/Objects/Overlays/AmmoCounter.events/Draw.xml
index 3ec574f8..35979af6 100644
--- a/Source/gg2/Objects/Overlays/AmmoCounter.events/Draw.xml
+++ b/Source/gg2/Objects/Overlays/AmmoCounter.events/Draw.xml
@@ -22,6 +22,7 @@ draw_set_alpha(1);
 
 if global.myself.object != -1 {
     if instance_exists(global.myself.object.currentWeapon) {
+        // TODO(enigma): read alarm[] via weapon, not global.myself.object.currentWeapon.alarm[] (ENIGMA nested built-in dot bug)
         var weapon;
         weapon = global.myself.object.currentWeapon;
         barcolor = make_color_rgb(217,217,183);
@@ -35,36 +36,36 @@ if global.myself.object != -1 {
         if global.myself.class==CLASS_SOLDIER {
             if global.myself.team == TEAM_BLUE toffset = 5;
             else toffset = 0;
-            reloadscalar = 100-global.myself.object.currentWeapon.alarm[5]*global.delta_factor/global.myself.object.currentWeapon.reloadTime*100;
+            reloadscalar = 100-weapon.alarm[5]*global.delta_factor/weapon.reloadTime*100;
             draw_sprite_ext(Rocketclip,global.myself.object.currentWeapon.ammoCount+toffset,xoffset+728,yoffset+ysize/1.26+86,2.4,2.4,0,c_white,100);
             draw_healthbar(xoffset+689,yoffset+ysize/1.26+90,xoffset+723,yoffset+ysize/1.26+98,reloadscalar,c_black,barcolor,barcolor,0,true,false);
         }
         else if global.myself.class==CLASS_DEMOMAN{        
-            reloadscalar = 100-global.myself.object.currentWeapon.alarm[5]*global.delta_factor/global.myself.object.currentWeapon.reloadTime*100;
+            reloadscalar = 100-weapon.alarm[5]*global.delta_factor/weapon.reloadTime*100;
             draw_sprite_ext(MinegunAmmoS,toffset,xoffset+728,yoffset+ysize/1.26+86,2.4,2.4,0,c_white,100);
             draw_text(xoffset+765,yoffset+ysize/1.26+95,global.myself.object.currentWeapon.ammoCount);
             draw_healthbar(xoffset+700,yoffset+ysize/1.26+90,xoffset+750,yoffset+ysize/1.26+98,reloadscalar,c_black,barcolor,barcolor,0,true,false);
         }
         else if global.myself.class==CLASS_SCOUT {
-            reloadscalar = 100-global.myself.object.currentWeapon.alarm[5]*global.delta_factor/global.myself.object.currentWeapon.reloadTime*100;
+            reloadscalar = 100-weapon.alarm[5]*global.delta_factor/weapon.reloadTime*100;
             draw_sprite_ext(ScattergunAmmoS,toffset,xoffset+728,yoffset+ysize/1.26+86,2.4,2.4,0,c_white,100);
             draw_text(xoffset+765,yoffset+ysize/1.26+95,global.myself.object.currentWeapon.ammoCount);
             draw_healthbar(xoffset+700,yoffset+ysize/1.26+90,xoffset+750,yoffset+ysize/1.26+98,reloadscalar,c_black,barcolor,barcolor,0,true,false);
         }
         else if global.myself.class==CLASS_ENGINEER {
-            reloadscalar = 100-global.myself.object.currentWeapon.alarm[5]*global.delta_factor/global.myself.object.currentWeapon.reloadTime*100;
+            reloadscalar = 100-weapon.alarm[5]*global.delta_factor/weapon.reloadTime*100;
             draw_sprite_ext(ShotgunAmmoS,toffset,xoffset+728,yoffset+ysize/1.26+86,2.4,2.4,0,c_white,100);
             draw_text(xoffset+765,yoffset+ysize/1.26+95,global.myself.object.currentWeapon.ammoCount);
             draw_healthbar(xoffset+700,yoffset+ysize/1.26+90,xoffset+750,yoffset+ysize/1.26+98,reloadscalar,c_black,barcolor,barcolor,0,true,false);
         }
         else if global.myself.class == CLASS_SPY {
-            reloadscalar = 100-global.myself.object.currentWeapon.alarm[5]*global.delta_factor/global.myself.object.currentWeapon.reloadTime*100;
+            reloadscalar = 100-weapon.alarm[5]*global.delta_factor/weapon.reloadTime*100;
             draw_sprite_ext(RevolverAmmoS,toffset,xoffset+728,yoffset+ysize/1.26+86,2.4,2.4,0,c_white,100);
             draw_text(xoffset+765,yoffset+ysize/1.26+95,global.myself.object.currentWeapon.ammoCount);
             draw_healthbar(xoffset+700,yoffset+ysize/1.26+90,xoffset+750,yoffset+ysize/1.26+98,reloadscalar,c_black,barcolor,barcolor,0,true,false);
         }
         else if global.myself.class == CLASS_MEDIC {
-            reloadscalar = 100-global.myself.object.currentWeapon.alarm[5]*global.delta_factor/global.myself.object.currentWeapon.reloadTime*100;
+            reloadscalar = 100-weapon.alarm[5]*global.delta_factor/weapon.reloadTime*100;
             draw_sprite_ext(NeedleAmmoS,toffset,xoffset+728,yoffset+ysize/1.26+86,2.4,2.4,0,c_white,100);
             draw_text(xoffset+765,yoffset+ysize/1.26+95,global.myself.object.currentWeapon.ammoCount);
             draw_healthbar(xoffset+700,yoffset+ysize/1.26+90,xoffset+750,yoffset+ysize/1.26+98,reloadscalar,c_black,barcolor,barcolor,0,true,false);
diff --git a/Source/gg2/Objects/Overlays/DeathCam.events/Alarm 0.xml b/Source/gg2/Objects/Overlays/DeathCam.events/Alarm 0.xml
index 9118ffd3..bfa9aa6c 100644
--- a/Source/gg2/Objects/Overlays/DeathCam.events/Alarm 0.xml	
+++ b/Source/gg2/Objects/Overlays/DeathCam.events/Alarm 0.xml	
@@ -21,18 +21,24 @@
 
 if(lastDamageSource == DAMAGE_SOURCE_SENTRYTURRET and killedby.sentry)
 {
-    view_xview=killedby.sentry.x-400;
-    view_yview=killedby.sentry.y-300;
-    hp=killedby.sentry.hp;
-    maxHp=killedby.sentry.maxHp;
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var killerSentry;
+    killerSentry = killedby.sentry;
+    view_xview=killerSentry.x-400;
+    view_yview=killerSentry.y-300;
+    hp=killerSentry.hp;
+    maxHp=killerSentry.maxHp;
     killmessage = "You were killed by the autogun of";
 }
 else if(killedby.object != -1)
 {
-    view_xview=killedby.object.x-400;
-    view_yview=killedby.object.y-300;
-    hp=killedby.object.hp;
-    maxHp=killedby.object.maxHp;
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var killerObject;
+    killerObject = killedby.object;
+    view_xview=killerObject.x-400;
+    view_yview=killerObject.y-300;
+    hp=killerObject.hp;
+    maxHp=killerObject.maxHp;
     killmessage = "You were killed by";
 }
 else
diff --git a/Source/gg2/Objects/Overlays/DeathCam.events/Create.xml b/Source/gg2/Objects/Overlays/DeathCam.events/Create.xml
index df835e21..ef31414f 100644
--- a/Source/gg2/Objects/Overlays/DeathCam.events/Create.xml
+++ b/Source/gg2/Objects/Overlays/DeathCam.events/Create.xml
@@ -15,7 +15,10 @@
       <arguments>
         <argument kind="STRING">cursor_sprite = -1;
 alarm[0] = 60 / global.delta_factor;
-alarm[2] = max(1, min(150/global.delta_factor, global.myself.alarm[5]));
+// TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+var myPlayer;
+myPlayer = global.myself;
+alarm[2] = max(1, min(150/global.delta_factor, myPlayer.alarm[5]));
 
 zoom=1;
 zooming=false;
diff --git a/Source/gg2/Objects/Overlays/MedicRadar.events/Draw.xml b/Source/gg2/Objects/Overlays/MedicRadar.events/Draw.xml
index ad17150e..d9109361 100644
--- a/Source/gg2/Objects/Overlays/MedicRadar.events/Draw.xml
+++ b/Source/gg2/Objects/Overlays/MedicRadar.events/Draw.xml
@@ -38,7 +38,10 @@ with(Character) {
             (bubbleImage == 45 || bubbleImage == 49) &amp;&amp;
             (x &gt; xOffset + width || x &lt; xOffset || y &gt; yOffset+height || y &lt; yOffset)) {
         draw_set_alpha(bubbleAlpha);
-        theta = point_direction(global.myself.object.x, global.myself.object.y, x, y)*pi/180;
+        // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+        var myObject;
+        myObject = global.myself.object;
+        theta = point_direction(myObject.x, myObject.y, x, y)*pi/180;
         if(theta &lt;= cornerDegree || theta &gt; 2*pi - cornerDegree){
             unknown = (width/2 - 38*cos(theta))*tan(theta);
             draw_sprite_ext(MedRadarArrow, floor(hp/maxHp * 19), xOffset + width - cos(theta)*38, yOffset+ height/2 -unknown, 1,1,theta*180/pi, c_white, bubbleAlpha);
diff --git a/Source/gg2/Objects/Overlays/SandwichHud.events/Draw.xml b/Source/gg2/Objects/Overlays/SandwichHud.events/Draw.xml
index 37403f50..44a9f158 100644
--- a/Source/gg2/Objects/Overlays/SandwichHud.events/Draw.xml
+++ b/Source/gg2/Objects/Overlays/SandwichHud.events/Draw.xml
@@ -33,8 +33,11 @@ draw_set_halign(fa_right);
 draw_set_alpha(1);
 barcolor = make_color_rgb(217,217,183);
 var sandwichScalar;
-if !(global.myself.object.canEat)
-    sandwichScalar = (1-(global.myself.object.alarm[6]/(global.myself.object.eatCooldown / global.delta_factor))) * 100
+// TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+var myObject;
+myObject = global.myself.object;
+if !(myObject.canEat)
+    sandwichScalar = (1-(myObject.alarm[6]/(myObject.eatCooldown / global.delta_factor))) * 100
 else
     sandwichScalar = 100
 draw_sprite_ext(sprite_index,offset,xoffset+xsize-70,yoffset+ysize-85,2,2,0,c_white,1);
diff --git a/Source/gg2/Objects/Weapons/Medigun.events/Draw.xml b/Source/gg2/Objects/Weapons/Medigun.events/Draw.xml
index 9d097eec..fa8a5d36 100644
--- a/Source/gg2/Objects/Weapons/Medigun.events/Draw.xml
+++ b/Source/gg2/Objects/Weapons/Medigun.events/Draw.xml
@@ -31,7 +31,10 @@ if(instance_exists(healTarget))
             color = c_blue;
             
         draw_set_alpha(0.3);
-        draw_line_width_color(xdrawpos+lengthdir_x(24,owner.aimDirection),ydrawpos+lengthdir_y(24,owner.aimDirection),healTarget.object.x, healTarget.object.y, 5, color, color);
+        // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+        var targetObject;
+        targetObject = healTarget.object;
+        draw_line_width_color(xdrawpos+lengthdir_x(24,owner.aimDirection),ydrawpos+lengthdir_y(24,owner.aimDirection),targetObject.x, targetObject.y, 5, color, color);
 
         sprite_index = normalSprite;
         imageOffset = owner.team+2;
diff --git a/Source/gg2/Objects/Weapons/Medigun.events/User Event 1.xml b/Source/gg2/Objects/Weapons/Medigun.events/User Event 1.xml
index 23b26c3e..2cbc0bc6 100644
--- a/Source/gg2/Objects/Weapons/Medigun.events/User Event 1.xml	
+++ b/Source/gg2/Objects/Weapons/Medigun.events/User Event 1.xml	
@@ -72,8 +72,11 @@
     
     if(instance_exists(healTarget)) {
         if(healTarget.object != -1) {
-            if(point_distance(x,y,healTarget.object.x, healTarget.object.y)&lt;=maxHealDistance) {
-                if(collision_line(x,y, healTarget.object.x, healTarget.object.y, Obstacle, true, true)&lt;0) {
+            // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+            var targetObject;
+            targetObject = healTarget.object;
+            if(point_distance(x,y,targetObject.x, targetObject.y)&lt;=maxHealDistance) {
+                if(collision_line(x,y, targetObject.x, targetObject.y, Obstacle, true, true)&lt;0) {
                     healedThisStep = true;
                     
                     if (healTarget.object.healer == -1)
@@ -113,7 +116,8 @@
                     } else if(ubering == true){
                         if healTarget.object.ubered == 0 playsound(x,y,UberStartSnd);
                         healTarget.object.ubered = 1;
-                        healTarget.object.alarm[2] = 3 / global.delta_factor; 
+                        // TODO(enigma): targetObject avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+                        targetObject.alarm[2] = 3 / global.delta_factor;
                     }
                                                                
                     if hphealed &gt;= HP_PER_POINT {
diff --git a/Source/gg2/Objects/Weapons/SentryTurret.events/Draw.xml b/Source/gg2/Objects/Weapons/SentryTurret.events/Draw.xml
index 1222a6fc..12bac81d 100644
--- a/Source/gg2/Objects/Weapons/SentryTurret.events/Draw.xml
+++ b/Source/gg2/Objects/Weapons/SentryTurret.events/Draw.xml
@@ -33,7 +33,10 @@ else
     
 if(ownerPlayer.sentry)
 {
-    if(ownerPlayer.sentry.image_index &gt; 7)
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var ownerSentry;
+    ownerSentry = ownerPlayer.sentry;
+    if(ownerSentry.image_index &gt; 7)
     {
         if(rotating)
             draw_sprite_ext(TurretRotateS,round(rotateindex),x,y,image_xscale,image_yscale,0,c_white,1);
diff --git a/Source/gg2/Objects/Weapons/SentryTurret.events/End Step.xml b/Source/gg2/Objects/Weapons/SentryTurret.events/End Step.xml
index d161347d..c0443e62 100644
--- a/Source/gg2/Objects/Weapons/SentryTurret.events/End Step.xml	
+++ b/Source/gg2/Objects/Weapons/SentryTurret.events/End Step.xml	
@@ -17,8 +17,11 @@
 
 if(ownerPlayer.sentry)
 {
-    x = ownerPlayer.sentry.x;
-    y = ownerPlayer.sentry.y;
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var ownerSentry;
+    ownerSentry = ownerPlayer.sentry;
+    x = ownerSentry.x;
+    y = ownerSentry.y;
 }
 else
 {
@@ -89,7 +92,11 @@ if(!nearestTarget) {
 } else {
     // set cloaked spy as target if they're in the way
     with(Spy) {
-        if(team != other.team &amp;&amp; collision_line(other.x,other.y,other.nearestTarget.x,other.nearestTarget.y,id,true,false)&gt;=0) {
+        // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+        // Re-read each iteration: an earlier spy may have become the target.
+        var turretTarget;
+        turretTarget = other.nearestTarget;
+        if(team != other.team &amp;&amp; collision_line(other.x,other.y,turretTarget.x,turretTarget.y,id,true,false)&gt;=0) {
             other.nearestTarget = id;
         }
     }
@@ -121,7 +128,10 @@ if(!nearestTarget) {
         
         playsound(x,y,ShotgunSnd);
         ownerPlayer.sentry.idleTimer = false;
-        ownerPlayer.sentry.alarm[5] = 30 / global.delta_factor;
+        // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+        var ownerSentry;
+        ownerSentry = ownerPlayer.sentry;
+        ownerSentry.alarm[5] = 30 / global.delta_factor;
         
         if(nearestTarget.ubered == 0) {
             damageAuto(ownerPlayer, nearestTarget, hitDamage);
diff --git a/Source/gg2/Scripts/Client/ClientBeginStep.gml b/Source/gg2/Scripts/Client/ClientBeginStep.gml
index a98211c4..6cf98171 100644
--- a/Source/gg2/Scripts/Client/ClientBeginStep.gml
+++ b/Source/gg2/Scripts/Client/ClientBeginStep.gml
@@ -583,11 +583,14 @@ do {
             player = ds_list_find_value(global.players, read_ubyte(global.tempBuffer));
             if(player.sentry)
             {
-                player.sentry.x = read_ushort(global.tempBuffer) / 5;
-                player.sentry.y = read_ushort(global.tempBuffer) / 5;
-                player.sentry.xprevious = player.sentry.x;
-                player.sentry.yprevious = player.sentry.y;
-                player.sentry.vspeed = 0;
+                // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+                var playerSentry;
+                playerSentry = player.sentry;
+                playerSentry.x = read_ushort(global.tempBuffer) / 5;
+                playerSentry.y = read_ushort(global.tempBuffer) / 5;
+                playerSentry.xprevious = playerSentry.x;
+                playerSentry.yprevious = playerSentry.y;
+                playerSentry.vspeed = 0;
             }
             break;
           
diff --git a/Source/gg2/Scripts/Events/doEventPlayerDeath.gml b/Source/gg2/Scripts/Events/doEventPlayerDeath.gml
index 7644adfa..769a56ba 100644
--- a/Source/gg2/Scripts/Events/doEventPlayerDeath.gml
+++ b/Source/gg2/Scripts/Events/doEventPlayerDeath.gml
@@ -14,6 +14,10 @@ assistant = argument2;
 damageSource = argument3;
 killersForDomination = ds_list_create();
 
+// TODO(enigma): victimObject avoids ENIGMA nested built-in dot bug (victim.object.x); inline when fixed
+var victimObject;
+victimObject = victim.object;
+
 if(!instance_exists(killer))
     killer = noone;
 
@@ -38,7 +42,10 @@ if(killer)
         killer.roundStats[POINTS] +=1;
     }
     
-    if (victim.object.currentWeapon.object_index == Medigun)
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var victimWeapon;
+    victimWeapon = victimObject.currentWeapon;
+    if (victimWeapon.object_index == Medigun)
     {
         if (victim.object.currentWeapon.uberReady)
         {
@@ -99,7 +106,7 @@ ds_list_destroy(killersForDomination);
 
 //SPEC
 if (victim == global.myself)
-    instance_create(victim.object.x, victim.object.y, Spectator);
+    instance_create(victimObject.x, victimObject.y, Spectator);
 
 //*************************************
 //*         Gibbing
@@ -264,27 +271,30 @@ with(victim.object) {
 }
 
 if (victim.object.has_crown) {
-    myHat = instance_create(victim.object.x,victim.object.y,CrownGib);
+    myHat = instance_create(victimObject.x,victimObject.y,CrownGib);
     myHat.image_index = victim.team;
 }
 if (victim.object.has_navigatorhat) {
-    myHat = instance_create(victim.object.x,victim.object.y,NavigatorHatGib);
+    myHat = instance_create(victimObject.x,victimObject.y,NavigatorHatGib);
     myHat.image_index = victim.team;
 }
 if (victim.object.has_partyhat){
-    myHat = instance_create(victim.object.x,victim.object.y,PartyHat);
+    myHat = instance_create(victimObject.x,victimObject.y,PartyHat);
     myHat.image_index = victim.team;
 }
 if (global.xmas){
-    myHat = instance_create(victim.object.x,victim.object.y,XmasHat);
+    myHat = instance_create(victimObject.x,victimObject.y,XmasHat);
     myHat.image_index = victim.team;
 }
 
 if (hasReward(victim, 'Ghost') and victim.ghost == noone) {
-    victim.ghost = instance_create(victim.object.x, victim.object.y, Ghost);
-    victim.ghost.owner = victim;
-    victim.ghost.hspeed = hspeed;
-    victim.ghost.vspeed = vspeed;
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var newGhost;
+    newGhost = instance_create(victimObject.x, victimObject.y, Ghost);
+    victim.ghost = newGhost;
+    newGhost.owner = victim;
+    newGhost.hspeed = hspeed;
+    newGhost.vspeed = vspeed;
 }
 
 with(victim.object) {       
diff --git a/Source/gg2/Scripts/Events/doEventUber.gml b/Source/gg2/Scripts/Events/doEventUber.gml
index 5a3fc81c..126fa6cf 100644
--- a/Source/gg2/Scripts/Events/doEventUber.gml
+++ b/Source/gg2/Scripts/Events/doEventUber.gml
@@ -8,7 +8,10 @@ var uberer;
 uberer = argument0;
                     
 if(uberer.object != -1) {
-    playsound(uberer.object.x,uberer.object.y,UberStartSnd);
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var ubererObject;
+    ubererObject = uberer.object;
+    playsound(ubererObject.x,ubererObject.y,UberStartSnd);
     with(uberer.object.currentWeapon) {
         ubering = true;
         uberReady = false;
diff --git a/Source/gg2/Scripts/Events/doEventUberReady.gml b/Source/gg2/Scripts/Events/doEventUberReady.gml
index 3f4cf8cd..a50eda21 100644
--- a/Source/gg2/Scripts/Events/doEventUberReady.gml
+++ b/Source/gg2/Scripts/Events/doEventUberReady.gml
@@ -8,7 +8,10 @@ var uberer;
 uberer = argument0;
  
 if(uberer.object != -1) {
-    playsound(uberer.object.x,uberer.object.y,UberChargedSnd);
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var ubererObject;
+    ubererObject = uberer.object;
+    playsound(ubererObject.x,ubererObject.y,UberChargedSnd);
     setChatBubble(uberer, 46);
     with(Medigun) {
         if(ownerPlayer == uberer) {
diff --git a/Source/gg2/Scripts/Events/sendEventFireWeapon.gml b/Source/gg2/Scripts/Events/sendEventFireWeapon.gml
index b32a364b..95576d28 100644
--- a/Source/gg2/Scripts/Events/sendEventFireWeapon.gml
+++ b/Source/gg2/Scripts/Events/sendEventFireWeapon.gml
@@ -6,9 +6,12 @@
 write_ubyte(global.sendBuffer, WEAPON_FIRE);
 write_ubyte(global.sendBuffer, ds_list_find_index(global.players, argument0));
 
-write_ushort(global.sendBuffer, argument0.object.x*5);
-write_ushort(global.sendBuffer, argument0.object.y*5);
-write_byte(global.sendBuffer, argument0.object.hspeed*8.5);
-write_byte(global.sendBuffer, argument0.object.vspeed*8.5);
+// TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+var playerObject;
+playerObject = argument0.object;
+write_ushort(global.sendBuffer, playerObject.x*5);
+write_ushort(global.sendBuffer, playerObject.y*5);
+write_byte(global.sendBuffer, playerObject.hspeed*8.5);
+write_byte(global.sendBuffer, playerObject.vspeed*8.5);
 
 write_ushort(global.sendBuffer, argument1);
diff --git a/Source/gg2/Scripts/Faucet HTTP/__http_parse_url.gml b/Source/gg2/Scripts/Faucet HTTP/__http_parse_url.gml
index 1a983f44..b5fdb4c7 100644
--- a/Source/gg2/Scripts/Faucet HTTP/__http_parse_url.gml	
+++ b/Source/gg2/Scripts/Faucet HTTP/__http_parse_url.gml	
@@ -59,7 +59,8 @@ while (string_char_at(url, 1) == '/')
     url = string_copy(url, 2, string_length(url) - 1);
 
 // before hostname
-var slashPos, colonPos;
+// colonPos is declared above; ENIGMA rejects redeclaring a var in the same scope
+var slashPos;
 // Find slash for beginning of path
 slashPos = string_pos('/', url);
 // No slash ahead - http://host format with no ending slash
diff --git a/Source/gg2/Scripts/GameServer/GameServerCreate.gml b/Source/gg2/Scripts/GameServer/GameServerCreate.gml
index 89b3024d..269795c8 100644
--- a/Source/gg2/Scripts/GameServer/GameServerCreate.gml
+++ b/Source/gg2/Scripts/GameServer/GameServerCreate.gml
@@ -89,7 +89,8 @@
     
     instance_create(0,0,PlayerControl);
 
-    var map, i;
+    // i is declared above; ENIGMA rejects redeclaring a var in the same scope
+    var map;
     if (global.shuffleRotation) {
         ds_list_shuffle(global.map_rotation);
         map = ds_list_find_value(global.map_rotation, 0);
diff --git a/Source/gg2/Scripts/GameServer/GameServerDefineCommands.gml b/Source/gg2/Scripts/GameServer/GameServerDefineCommands.gml
index 3bde42c7..17e71ecf 100644
--- a/Source/gg2/Scripts/GameServer/GameServerDefineCommands.gml
+++ b/Source/gg2/Scripts/GameServer/GameServerDefineCommands.gml
@@ -1,8 +1,9 @@
 var i;
 
-commandBytesInvalidCommand = -1; // No such command
-commandBytesPrefixLength1 = -2;  // The length of the command is indicated by the first byte
-commandBytesPrefixLength2 = -3;  // The length of the command is indicated by the first two bytes
+// commandBytes* sentinels are constants (Constants.xml): ENIGMA needs compile-time case labels.
+// commandBytesInvalidCommand: no such command
+// commandBytesPrefixLength1: the length of the command is indicated by the first byte
+// commandBytesPrefixLength2: the length of the command is indicated by the first two bytes
 
 for(i=0; i<256; i+=1) {
     // -1 indicates an invalid command byte
diff --git a/Source/gg2/Scripts/GameServer/processClientCommands.gml b/Source/gg2/Scripts/GameServer/processClientCommands.gml
index 5f2b5b11..919134f1 100644
--- a/Source/gg2/Scripts/GameServer/processClientCommands.gml
+++ b/Source/gg2/Scripts/GameServer/processClientCommands.gml
@@ -212,19 +212,22 @@ while(commandLimitRemaining > 0) {
         case BUILD_SENTRY:
             if(player.object != -1)
             {
+                // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+                var playerObject;
+                playerObject = player.object;
                 if(player.class == CLASS_ENGINEER
-                        and collision_circle(player.object.x, player.object.y, 50, Sentry, false, true) < 0
-                        and player.object.nutsNBolts == 100
-                        and (collision_point(player.object.x,player.object.y,SpawnRoom,0,0) < 0)
+                        and collision_circle(playerObject.x, playerObject.y, 50, Sentry, false, true) < 0
+                        and playerObject.nutsNBolts == 100
+                        and (collision_point(playerObject.x,playerObject.y,SpawnRoom,0,0) < 0)
                         and !player.sentry
-                        and !player.object.onCabinet)
+                        and !playerObject.onCabinet)
                 {
                     write_ubyte(global.sendBuffer, BUILD_SENTRY);
                     write_ubyte(global.sendBuffer, playerId);
-                    write_ushort(global.serializeBuffer, round(player.object.x*5));
-                    write_ushort(global.serializeBuffer, round(player.object.y*5));
-                    write_byte(global.serializeBuffer, player.object.image_xscale);
-                    buildSentry(player, player.object.x, player.object.y, player.object.image_xscale);
+                    write_ushort(global.serializeBuffer, round(playerObject.x*5));
+                    write_ushort(global.serializeBuffer, round(playerObject.y*5));
+                    write_byte(global.serializeBuffer, playerObject.image_xscale);
+                    buildSentry(player, playerObject.x, playerObject.y, playerObject.image_xscale);
                 }
             }
             break;                                       
diff --git a/Source/gg2/Scripts/Gear/setBasicHeadPoses.gml b/Source/gg2/Scripts/Gear/setBasicHeadPoses.gml
index 49871131..581e268c 100644
--- a/Source/gg2/Scripts/Gear/setBasicHeadPoses.gml
+++ b/Source/gg2/Scripts/Gear/setBasicHeadPoses.gml
@@ -27,21 +27,18 @@ for(team = 0; team < 2; team += 1)
         animation = ds_list_find_value(basicAnimations, j);
         spriteId = getCharacterSpriteId(className, team, animation);
         
-        switch(animation)
+        // TODO(enigma): if/else instead of switch; ENIGMA can't switch on strings. Restore switch when fixed
+        if (animation == "Run")
         {
-        case "Run":
             setHeadPose(spriteId, 0, dxBase, dyBase-bobAmount, 0, 1);
             setHeadPose(spriteId, 1, dxBase, dyBase, 0, 1);
             setHeadPose(spriteId, 2, dxBase, dyBase-bobAmount, 0, 1);
             setHeadPose(spriteId, 3, dxBase, dyBase, 0, 1);
-            break;
-        case "Taunt":
+        }
+        else if (animation == "Taunt")
             setHeadPosesFromString(spriteId, dxBase, dyBase, tauntPoseString);
-            break;
-        default:
+        else
             setHeadPose(spriteId, 0, dxBase, dyBase, 0, 1);
-            break;
-        }
     }
 }
 
diff --git a/Source/gg2/Scripts/Input/inputSpecial.gml b/Source/gg2/Scripts/Input/inputSpecial.gml
index 8c361896..019e2b98 100644
--- a/Source/gg2/Scripts/Input/inputSpecial.gml
+++ b/Source/gg2/Scripts/Input/inputSpecial.gml
@@ -1,5 +1,8 @@
 if(global.myself.class == CLASS_ENGINEER)
 {
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var myObject;
+    myObject = global.myself.object;
     if(global.myself.sentry)
     {
         write_ubyte(global.serverSocket, DESTROY_SENTRY);
@@ -12,14 +15,16 @@ if(global.myself.class == CLASS_ENGINEER)
         instance_create(0,0,NoticeO);
         NoticeO.notice = NOTICE_NUTSNBOLTS;
     }
-    else if(collision_circle(global.myself.object.x,global.myself.object.y,50,Sentry,false,true)>=0)
+    // TODO(enigma): myObject avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    else if(collision_circle(myObject.x,myObject.y,50,Sentry,false,true)>=0)
     {
         with(NoticeO)
             instance_destroy();
         instance_create(0,0,NoticeO);
         NoticeO.notice = NOTICE_TOOCLOSE;
     }
-    else if(collision_point(global.myself.object.x,global.myself.object.y,SpawnRoom,0,0) < 0)
+    // TODO(enigma): myObject avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    else if(collision_point(myObject.x,myObject.y,SpawnRoom,0,0) < 0)
     {
         write_ubyte(global.serverSocket, BUILD_SENTRY);
         socket_send(global.serverSocket);
diff --git a/Source/gg2/Scripts/Kill Log/recordDestructionInLog.gml b/Source/gg2/Scripts/Kill Log/recordDestructionInLog.gml
index 0eee9bae..c6335a2c 100644
--- a/Source/gg2/Scripts/Kill Log/recordDestructionInLog.gml	
+++ b/Source/gg2/Scripts/Kill Log/recordDestructionInLog.gml	
@@ -37,18 +37,14 @@
             
             ds_map_add(map, "weapon", findDamageSourceIcon(argument3));
             
-            switch(argument3) {
-                case DAMAGE_SOURCE_FINISHED_OFF:
-                case DAMAGE_SOURCE_FINISHED_OFF_GIB:
-                    ds_map_add(map, "string", "finished off ");
-                    break;
-                case DAMAGE_SOURCE_BID_FAREWELL:
-                    ds_map_add(map, "string", string_copy(argument0.name, 1, 20) + " bid farewell, cruel world!");
-                    break;
-                default:
-                    ds_map_add(map, "string", "");
-                    break;
-            }
+            // TODO(enigma): if/else instead of switch; ENIGMA needs compile-time case labels and
+            // DAMAGE_SOURCE_* are assigned at runtime. Restore switch when fixed
+            if (argument3 == DAMAGE_SOURCE_FINISHED_OFF or argument3 == DAMAGE_SOURCE_FINISHED_OFF_GIB)
+                ds_map_add(map, "string", "finished off ");
+            else if (argument3 == DAMAGE_SOURCE_BID_FAREWELL)
+                ds_map_add(map, "string", string_copy(argument0.name, 1, 20) + " bid farewell, cruel world!");
+            else
+                ds_map_add(map, "string", "");
             
             ds_list_add(kills, map);
             
diff --git a/Source/gg2/Scripts/Kill Log/recordKillInLog.gml b/Source/gg2/Scripts/Kill Log/recordKillInLog.gml
index 30493d2c..95aa3598 100644
--- a/Source/gg2/Scripts/Kill Log/recordKillInLog.gml	
+++ b/Source/gg2/Scripts/Kill Log/recordKillInLog.gml	
@@ -36,21 +36,16 @@
             
             ds_map_add(map, "weapon", findDamageSourceIcon(argument3));
             
-            switch(argument3) {
-                case DAMAGE_SOURCE_PITFALL:
-                    ds_map_add(map, "string", string_copy(argument0.name, 1, 20) + " fell to a clumsy, painful death.");
-                    break;
-                case DAMAGE_SOURCE_FINISHED_OFF:
-                case DAMAGE_SOURCE_FINISHED_OFF_GIB:
-                    ds_map_add(map, "string", "finished off ");
-                    break;
-                case DAMAGE_SOURCE_BID_FAREWELL:
-                    ds_map_add(map, "string", string_copy(argument0.name, 1, 20) + " bid farewell, cruel world!");
-                    break;
-                default:
-                    ds_map_add(map, "string", "");
-                    break;
-            }
+            // TODO(enigma): if/else instead of switch; ENIGMA needs compile-time case labels and
+            // DAMAGE_SOURCE_* are assigned at runtime. Restore switch when fixed
+            if (argument3 == DAMAGE_SOURCE_PITFALL)
+                ds_map_add(map, "string", string_copy(argument0.name, 1, 20) + " fell to a clumsy, painful death.");
+            else if (argument3 == DAMAGE_SOURCE_FINISHED_OFF or argument3 == DAMAGE_SOURCE_FINISHED_OFF_GIB)
+                ds_map_add(map, "string", "finished off ");
+            else if (argument3 == DAMAGE_SOURCE_BID_FAREWELL)
+                ds_map_add(map, "string", string_copy(argument0.name, 1, 20) + " bid farewell, cruel world!");
+            else
+                ds_map_add(map, "string", "");
             
             ds_list_add(kills, map);
             
diff --git a/Source/gg2/Scripts/Maps/basicRoomSetup.gml b/Source/gg2/Scripts/Maps/basicRoomSetup.gml
index e51209f5..43186c69 100644
--- a/Source/gg2/Scripts/Maps/basicRoomSetup.gml
+++ b/Source/gg2/Scripts/Maps/basicRoomSetup.gml
@@ -10,7 +10,10 @@ if global.totalMapAreas > 1 {
     global.area[1] = 0;
     
     for(i=2;i<=global.totalMapAreas;i+=1) {
-        global.area[i] = instance_find(NextAreaO,i-2).y;
+        // TODO(enigma): temp var avoids ENIGMA dot-after-call bug (f().y); inline when fixed
+        var nextArea;
+        nextArea = instance_find(NextAreaO,i-2);
+        global.area[i] = nextArea.y;
     }
 
     if (global.currentMapArea == 1)
diff --git a/Source/gg2/Scripts/Maps/findInternalMapName.gml b/Source/gg2/Scripts/Maps/findInternalMapName.gml
index 9f57afd6..8994a24c 100644
--- a/Source/gg2/Scripts/Maps/findInternalMapName.gml
+++ b/Source/gg2/Scripts/Maps/findInternalMapName.gml
@@ -1,56 +1,51 @@
 // returns the filename of an internal map by name, or else (if it's not a valid internal map), ""
 
-switch(argument0)
-{
-case "ctf_truefort":
+// TODO(enigma): if chain instead of switch; ENIGMA can't switch on strings. Restore switch when fixed
+var name;
+name = argument0;
+
+if (name == "ctf_truefort")
     return "ctf_truefort.png";
-case "ctf_2dfort":
-case "ctf_2dfort2":
-case "ctf_2dfortremix":
+if (name == "ctf_2dfort" or name == "ctf_2dfort2" or name == "ctf_2dfortremix")
     return "ctf_2dfort.png";
-case "ctf_conflict":
+if (name == "ctf_conflict")
     return "ctf_conflict.png";
-case "ctf_classicwell":
+if (name == "ctf_classicwell")
     return "ctf_classicwell.png";
-case "ctf_waterway":
+if (name == "ctf_waterway")
     return "ctf_waterway.png";
-case "ctf_orange":
+if (name == "ctf_orange")
     return "ctf_orange.png";
-case "ctf_avanti":
+if (name == "ctf_avanti")
     return "ctf_avanti.png";
-case "cp_dirtbowl":
+if (name == "cp_dirtbowl")
     return "cp_dirtbowl.png";
-case "cp_egypt":
+if (name == "cp_egypt")
     return "cp_egypt.png";
-case "arena_montane":
+if (name == "arena_montane")
     return "arena_montane.png";
-case "arena_lumberyard":
+if (name == "arena_lumberyard")
     return "arena_lumberyard.png";
-case "gen_destroy":
+if (name == "gen_destroy")
     return "gen_destroy.png";
-case "koth_harvest":
+if (name == "koth_harvest")
     return "koth_harvest.png";
-case "koth_valley":
+if (name == "koth_valley")
     return "koth_valley.png";
-case "koth_corinth":
+if (name == "koth_corinth")
     return "koth_corinth.png";
-case "dkoth_atalia":
+if (name == "dkoth_atalia")
     return "dkoth_atalia.png";
-case "dkoth_sixties":
-case "dkoth_60s":
+if (name == "dkoth_sixties" or name == "dkoth_60s")
     return "dkoth_sixties.png";
-case "tdm_mantic":
+if (name == "tdm_mantic")
     return "tdm_mantic.png";
-case "ctf_2dfort[0]":
-case "ctf_2dfort1":
-case "ctf_oldfort":
+if (name == "ctf_2dfort[0]" or name == "ctf_2dfort1" or name == "ctf_oldfort")
     return "ctf_oldfort.png";
-case "gg_debug":
+if (name == "gg_debug")
     return "gg_debug.png";
-case "koth_gallery":
+if (name == "koth_gallery")
     return "koth_gallery.png";
-case "ctf_eiger":
+if (name == "ctf_eiger")
     return "ctf_eiger.png";
-default:
-    return "";
-}
+return "";
diff --git a/Source/gg2/Scripts/Misc/Lists/ds_list_turn.gml b/Source/gg2/Scripts/Misc/Lists/ds_list_turn.gml
index 725e7831..c464c255 100644
--- a/Source/gg2/Scripts/Misc/Lists/ds_list_turn.gml
+++ b/Source/gg2/Scripts/Misc/Lists/ds_list_turn.gml
@@ -1,6 +1,6 @@
 // Reads the first value in a list, then moves it to the back of the list
 if(ds_list_size(argument0) < 0)
-    return NAN;
+    return 0; // unreachable; was NAN, which neither GM8 nor ENIGMA defines
 
 var n;
 n = ds_list_find_value(argument0, 0);
diff --git a/Source/gg2/Scripts/Misc/setChatBubble.gml b/Source/gg2/Scripts/Misc/setChatBubble.gml
index 57b1408f..4521e0ab 100644
--- a/Source/gg2/Scripts/Misc/setChatBubble.gml
+++ b/Source/gg2/Scripts/Misc/setChatBubble.gml
@@ -1,9 +1,12 @@
 if(instance_exists(argument0)) {
     if(argument0.object != -1) {
-        argument0.object.bubble.image_index = argument1;
-        argument0.object.bubble.alarm[0] = 60 / global.delta_factor;
-        argument0.object.bubble.visible = true;
-        argument0.object.bubble.bubbleAlpha = 1;
-        argument0.object.bubble.fadeout = false;
+        // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+        var chatBubble;
+        chatBubble = argument0.object.bubble;
+        chatBubble.image_index = argument1;
+        chatBubble.alarm[0] = 60 / global.delta_factor;
+        chatBubble.visible = true;
+        chatBubble.bubbleAlpha = 1;
+        chatBubble.fadeout = false;
     }
 }
diff --git a/Source/gg2/Scripts/Object Creation/buildSentry.gml b/Source/gg2/Scripts/Object Creation/buildSentry.gml
index 5eefc183..656247fa 100644
--- a/Source/gg2/Scripts/Object Creation/buildSentry.gml	
+++ b/Source/gg2/Scripts/Object Creation/buildSentry.gml	
@@ -13,10 +13,16 @@ if(!player.sentry)
 }
 else
 {
-    player.sentry.x = xPos;
-    player.sentry.y = yPos;
+    // TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+    var playerSentry;
+    playerSentry = player.sentry;
+    playerSentry.x = xPos;
+    playerSentry.y = yPos;
 }
 
-player.sentry.startDirection = startDirection;
-player.sentry.image_xscale = startDirection;
+// TODO(enigma): temp var avoids ENIGMA nested built-in dot bug (a.b.x); inline when fixed
+var sentryInst;
+sentryInst = player.sentry;
+sentryInst.startDirection = startDirection;
+sentryInst.image_xscale = startDirection;
 player.object.nutsNBolts -= 100;
diff --git a/Source/gg2/Scripts/ggon/ggon_tokenise.gml b/Source/gg2/Scripts/ggon/ggon_tokenise.gml
index 6f68ad50..c9c14092 100644
--- a/Source/gg2/Scripts/ggon/ggon_tokenise.gml
+++ b/Source/gg2/Scripts/ggon/ggon_tokenise.gml
@@ -16,25 +16,19 @@ while (i <= len)
 {
     var char;
     char = string_char_at(text, i);
-    switch (char)
+    // TODO(enigma): if/else instead of switch; ENIGMA can't switch on strings. Restore switch when fixed
+    // basic punctuation: '{', '}', ':', '[', ']', and ','
+    if (char == '{' or char == '}' or char == ':' or char == '[' or char == ']' or char == ',')
     {
-        // basic punctuation: '{', '}', ':', '[', ']', and ','
-        case '{':
-        case '}':
-        case ':':
-        case '[':
-        case ']':
-        case ',':
-            ds_queue_enqueue(tokens, char);
-            i += 1;
-            continue;
-        // skip whitespace (space, tab, new line or carriage return)
-        case ' ':
-        case chr(9):
-        case chr(10):
-        case chr(13):
-            i += 1;
-            continue;
+        ds_queue_enqueue(tokens, char);
+        i += 1;
+        continue;
+    }
+    // skip whitespace (space, tab, new line or carriage return)
+    if (char == ' ' or char == chr(9) or char == chr(10) or char == chr(13))
+    {
+        i += 1;
+        continue;
     }
     // "identifiers" (bare word strings, really) of format [a-zA-Z0-9_]+
     if (('a' <= char and char <= 'z') or ('A' <= char and char <= 'Z') or ('0' <= char and char <= '9') or char == '_' or char == '.' or char == '+' or char == '-')
